<navigation-bar
  title="高级模板"
  back="{{true}}"
  color="#24160f"
  background="rgba(255,253,248,0.96)"
  ext-class="pro-nav"
></navigation-bar>

<view class="pro-page">
  <scroll-view class="pro-scroll" scroll-y enhanced show-scrollbar="{{false}}">
    <view class="pro-chip-row">
      <view
        wx:for="{{filters}}"
        wx:key="id"
        class="pro-chip {{item.active ? 'pro-chip-active' : ''}}"
        data-id="{{item.id}}"
        bindtap="handleFilterTap"
      >{{item.name}}</view>
    </view>

    <view class="pro-list">
      <view wx:for="{{visibleTemplates}}" wx:key="id" class="pro-item" data-id="{{item.id}}" data-title="{{item.title}}" bindtap="handleTemplateTap">
        <view class="pro-thumb">
          <image src="{{item.imageUrl}}" mode="aspectFill"></image>
        </view>
        <view class="pro-copy">
          <text class="pro-title">{{item.title}}</text>
          <text class="pro-meta">{{item.meta}}</text>
          <text class="pro-meta">{{pendingTemplateId === item.id ? '处理中…' : item.accessText}}</text>
        </view>
        <view class="pro-tag">{{item.locked ? '已锁定' : (membershipEnabled && membershipActive ? '会员' : '可用')}}</view>
      </view>
    </view>

    <view class="pro-unlock-card">
      <view class="pro-unlock-head">
        <text class="pro-unlock-title">{{unlockTitle}}</text>
        <view class="pro-tag">{{unlockProgressText}}</view>
      </view>
      <view class="pro-primary" bindtap="handleUnlockTap">
        {{membershipEnabled ? (membershipActive ? '会员已解锁' : '开通会员解锁更多权益') : '会员入口已关闭'}}
      </view>
    </view>
  </scroll-view>
</view>
