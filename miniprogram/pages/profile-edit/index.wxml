<navigation-bar title="微信登录 / 编辑资料" back="{{true}}" color="#24160f" background="rgba(255,253,248,0.96)"></navigation-bar>

<view class="profile-edit-page">
  <scroll-view class="profile-edit-scroll" scroll-y enhanced show-scrollbar="{{false}}">
    <form class="profile-edit-avatar-card" bindsubmit="handleWechatProfileTap">
      <button class="profile-edit-avatar-picker" open-type="chooseAvatar" bindchooseavatar="handleChooseAvatar">
        <image class="profile-edit-avatar" src="{{avatarUrl}}" mode="aspectFill"></image>
        <text class="profile-edit-avatar-tip">{{hasWechatProfile ? '重新选择微信头像' : '选择微信头像'}}</text>
      </button>

      <view class="profile-edit-form-card profile-edit-form-card-inline">
        <view class="profile-edit-field">
          <text class="profile-edit-label">登录状态</text>
          <view class="profile-edit-value">{{loggedIn ? '已登录微信' : '未登录'}}</view>
        </view>
        <view class="profile-edit-field">
          <text class="profile-edit-label">微信昵称</text>
          <input
            class="profile-edit-input"
            type="nickname"
            name="nickname"
            value="{{name}}"
            placeholder="请输入微信昵称"
            bindinput="handleNicknameInput"
            bindblur="handleNicknameBlur"
          />
        </view>
        <view class="profile-edit-field">
          <text class="profile-edit-label">手机号</text>
          <view class="profile-edit-value">{{phoneMasked || '未绑定（可选）'}}</view>
        </view>
        <view class="profile-edit-field">
          <text class="profile-edit-label">唯一身份</text>
          <view class="profile-edit-value">{{loggedIn ? '微信 OpenID 已后台自动绑定' : '登录后自动生成'}}</view>
        </view>
      </view>

      <button class="profile-edit-primary" form-type="submit">1. 使用微信头像昵称登录</button>
      <button class="profile-edit-primary" open-type="getPhoneNumber" bindgetphonenumber="handlePhoneBind">2. 可选绑定手机号</button>
    </form>

    <view class="profile-edit-form-card">
      <view class="profile-edit-field">
        <text class="profile-edit-label">个性签名</text>
        <input class="profile-edit-input" value="{{signature}}" data-field="signature" bindinput="handleFieldInput" />
      </view>
      <view class="profile-edit-field">
        <text class="profile-edit-label">常用身份</text>
        <input class="profile-edit-input" value="{{identityTag}}" data-field="identityTag" bindinput="handleFieldInput" />
      </view>
    </view>

    <view class="profile-edit-primary" bindtap="handleSaveTap">保存资料</view>
  </scroll-view>
</view>
