# 聚会记录师 PRD

版本：2026-06-22

历史项目名：酒桌判官。面向用户的产品名统一为“聚会记录师”。

## 1. 产品定位

聚会记录师是一款微信小程序，用于线下聚会的快速开局、拍照记录、账本记录、时间线回忆、相册沉淀和分享图生成。

产品核心不是线上游戏，也不是单纯记账工具，而是让用户在聚会中用最少操作留下可分享的回忆。默认主路径必须围绕“创建聚会、邀请好友、拍第一张照片、记录过程、生成分享图、保存/分享”闭环。

## 2. 项目边界

### 2.1 技术边界

- 小程序前端：`miniprogram/`
- 后端服务：`backend/server.js`
- 后台管理：`backend/public/admin/static/heatwave-ops/`
- API 与后台目标域名：`https://api.pomer.cn`
- 公司官网 `pomer.cn` 不属于本项目，禁止修改、重启、部署、代理或路由。

### 2.2 文案边界

- 用户可见名称：聚会记录师
- 历史名“酒桌判官”只能出现在旧代码名、旧数据、旧文档或技术迁移上下文里。
- 新增 UI、后台新 IA、测试报告、对外文案不得继续强化“判官/惩罚/酒桌游戏”作为主心智。

## 3. 目标用户与场景

### 3.1 用户

- 聚会发起人：创建聚会、邀请好友、拍照、调整账本、结束聚会、生成分享图。
- 参与者：加入聚会、查看记录和相册、参与账本展示、查看或保存分享内容。
- 管理员：在后台查看聚会、用户、记录、分享图、举报、权益、积分、模板和运营数据。

### 3.2 核心场景

- 周末朋友聚会、饭局、酒局、烧烤局、生日局、团建局。
- 用户希望快速开始，不想先配置复杂规则。
- 聚会结束后，用户希望生成酷炫分享图或回忆海报，保存到相册或分享给好友/群。

## 4. 核心目标

1. 三步内创建聚会并拍第一张照片。
2. 邀请卡能让好友看懂口令和加入状态，好友点击可加入同一局。
3. 记录页以时间线承载所有事件：拍照、账本变动、关键文案、结束聚会等。
4. 聚会账本内置在记录、相册、分享同级 tab 中，可编辑并确认写入时间线。
5. 相册直接展示用户上传图片，不再走审核图片机制；图片可进入分享链路。
6. 分享图和分享预览是最重要体验，必须基于真实照片、账本、时间线、二维码生成，不得用假状态或默认图冒充。
7. 聚会必须有“进行中 / 已结束”两类状态，结束后记录保留，并进入个人中心与后台管理。
8. 后台管理必须能按状态查阅聚会和记录，管理员可看到必要关联数据。

## 5. 主链路

### 5.1 首页

首页只承载干净入口：

- 创建聚会
- 当前进行中聚会入口，仅在存在有效进行中聚会时展示
- 最近相册，封面优先使用该聚会上传的第一张照片，无照片使用默认封面
- 底部导航：首页、工具箱、排行榜、我的

首页不常驻展示口令输入、相册、分享、聚会账本等底部杂入口。无进行中聚会时，不展示“聚会待开局/继续记录”类常驻卡片。

### 5.2 创建聚会

创建页应轻量：

- 聚会名支持预制短文案点击填入，也可手动编辑
- 聚会人数可编辑
- 时间默认使用当前实时时间，并用于聚会列表展示
- 去掉轻量主题选择，不阻塞开局
- 创建成功后进入邀请页或首拍路径

### 5.3 邀请好友

邀请页必须保留邀请卡：

- 展示口令
- 展示已加入玩家头像
- 按创建时人数展示空态头像槽位
- 可手动刷新加入状态
- 可分享给好友，好友点击后加入该局

邀请卡内容要简洁，不堆说明性板块。口令加入状态区域不再重复放“分享给好友/分享到群/保存海报”等多按钮。

### 5.4 拍第一张照片

拍第一张上传成功后，点击保存照片应进入聚会进行中页面，而不是返回首页。

照片描述输入要低负担：

- 一句话说明收缩到两行内
- 下方给默认可选文案，点击即可填充
- 可见与授权默认选项中四项均选中
- 去掉“仅自己”选项

### 5.5 记录 / 相册 / 分享 / 账本

进入本局后，使用同一聚会页承载四个同级 tab：

- 记录
- 相册
- 分享
- 聚会账本

页面标题使用用户创建的聚会名，不使用固定图片标题。

#### 记录 tab

记录页使用左侧明线时间线，节点包含：

- 拍照事件
- 账本变动
- 用户输入的关键文案
- 分享图生成状态
- 结束聚会

单个节点要紧凑，避免过大导致下滑疲劳。照片节点必须显示真实照片，点击可查看大图，再点击或返回回到原页面。

记录页至少有：

- 继续拍照
- 结束聚会

结束聚会是状态收口，不是退出。点击后弹出“确认结束聚会”提示，确认后聚会状态变为已结束。

#### 相册 tab

相册展示本局所有可见照片：

- 直接展示已上传照片
- 不再走审核图片机制
- 图片可点击查看大图并返回
- 与记录页照片来源一致

#### 分享 tab

分享页展示分享图生成与保存状态：

- 分享预览主区域应展示时间线节点，而不是长而杂的卡片堆叠
- 按钮只保留：返回聚会、保存聚会图、刷新状态
- 生成中时，保存按钮显示“生成中”
- 刷新状态后，如果成功，主按钮变为“去分享”
- 生成失败时显示重新生成按钮
- 成功/失败状态放在按钮下方一行小字
- 分享图底部应包含小程序二维码

分享页不得展示假时间、假状态、默认二维码、默认图或无效可保存状态。

#### 聚会账本 tab

账本页展示原酒桌判官应有的欠酒/加酒数据，但作为聚会记录的内置功能：

- 展示真实用户头像和用户名
- 发起人可操作加减酒
- 参与者只读
- 加减操作先临时修改
- 点击“确定修改”后写入后端
- 修改成功后，记录 tab 时间线立即出现账本变动节点
- 页面保持清洁，避免重复按钮

## 6. 个人中心

个人中心以“我的回忆”为核心。

### 6.1 统计

统计字段：

- 总回忆数：用户所有状态下的聚会记录数
- 进行中：当前仍可继续记录的聚会数
- 已结束：已结束且记录保留的聚会数

已结束不能显示为 0，除非接口和后台均证明没有已结束记录。

### 6.2 列表

- 相册数 / 回忆数入口展示所有聚会记录，进入后按进行中、已结束等状态分类
- 待分享回忆只放用户没有生成过分享图且仍未结束的聚会
- 分享图入口展示用户已生成的分享图合集，用户可点击打开
- 如果待分享已经在下方出现，顶部不重复展示

个人中心避免重复入口，不能多个按钮都跳同一个相册页。

## 7. 工具箱与排行榜

### 7.1 工具箱

首页底部“账本”入口改为“工具箱”。工具箱沿用旧酒桌判官工具能力，但以新项目风格承载。

工具箱列表内每个工具必须可使用或明确标注不可用原因，不能保留死入口。

### 7.2 排行榜

排行榜放入底部导航，与首页、工具箱、我的同级。

排行榜展示今日回忆榜、聚会活跃、分享等与聚会记录相关的数据，不恢复旧酒桌玩法心智为主入口。

## 8. 功能区迁移

以下能力迁移到聚会记录师新 IA 中，不批量恢复旧孤岛页：

- 权益
- 会员
- 商户
- 积分
- 收藏
- 使用记录
- 邀请奖励
- 高级模板

这些能力在后台按功能区划分。若合同、后台配置、流水、日志或风控证据不足，前端应展示明确禁用原因，不得假成功。

## 9. 后台管理

后台应支持管理员查阅和管理：

- 用户资料
- 聚会记录
- 进行中 / 已结束状态
- 创建人、成员、邀请码
- 结束时间
- 照片、时间线、账本事件
- report / brief / share task 关联
- 分享图生成状态
- 举报与反馈
- 权益、会员、积分、商户、模板、工具配置
- 操作日志

当前 P0 要求：已结束记录必须保留在后台管理中，管理员能查阅。

## 10. 数据与状态合同

### 10.1 聚会状态

每个聚会必须至少有：

- `进行中`
- `已结束`

结束聚会后：

- 不删除 session
- 写入 `state/status/stateText=已结束`
- 写入 `endedAt/updatedAt`
- `canResume=false`
- 个人中心、相册分类、后台管理同步可见

### 10.2 用户摘要

`GET /api/v1/user/session-moment-summaries` 必须稳定返回：

- `sessionId`
- `sessionName`
- `state`
- `stateText`
- `status`
- `endedAt`
- `updatedAt`
- `canResume`
- `briefId`
- `shareImageTaskId`
- `readyShareImageUrl`

前端不得用本地假状态硬凑统计。

### 10.3 图片

照片 URL 必须是真实可访问图片：

- 不接受空 URL
- 不接受 1x1 白图冒充
- 不接受默认图冒充用户照片
- 不接受 `/assets` 占位图进入真实照片链路

## 11. 视觉与体验原则

- 全页面逐步统一为当前“我的”页面风格体系
- 不允许旧框套框、卡套卡、厚重列表和旧样式习惯
- 分享页、记录页、个人中心是视觉重点
- UI 提供素材、背景、光效、按钮、状态和标注后，前端必须 1:1 复刻
- 素材不足时前端应标阻塞，不得自行脑补样式
- 包体需控制在微信小程序限制内，资源可压缩和裁尺寸，但不得重绘替换已确认素材

## 12. 验收口径

当前开发阶段以微信开发者工具右侧预览框为主：

- 页面可点击
- 页面可返回
- 关键链路无阻塞红错
- Console / Network / storage 可解释
- 核心数据不是假状态

正式上线前还需补：

- 体验版或真机最终采集
- 线上 `api.pomer.cn` 成员态有效 token smoke
- 后台登录态 smoke
- UGC / 隐私 / 授权 / 分享图保存证据
- 数据埋点和后台报表对账

## 13. 当前最高优先级

P0：个人中心“已结束”统计为 0。

必须由后端/API、接口联调、后台管理、前端、测试验收共同闭环：

- 后端/API：确认摘要接口返回结束态字段
- 接口联调：对账 summary、liveSessions、reports、后台 sessions
- 后台管理：确认已结束记录可查
- 前端：确认统计消费不误归类
- 测试验收：确认个人中心、相册分类、后台三方一致

该问题未闭环前，不得写发布候选通过或上线准出。
