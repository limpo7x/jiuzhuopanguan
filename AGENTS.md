# 酒桌判官协作规则

请用中文沟通和提交说明。

## 产品改版命名规则

- 从 2026-06-15 起，面向用户的新产品名统一为“聚会记录师”。
- “酒桌判官”只作为历史项目名、旧代码模块名、旧文档名或技术上下文保留；新增 UI 文案、改版需求、测试报告、设计评审和对外描述必须使用“聚会记录师”。
- 改版方向从“酒桌游戏/判官/欠酒/惩罚”转为“聚会记录/拍照/相册/分享/回忆”。玩法、惩罚、积分和榜单降为可选或二级功能，不得阻塞默认创建与拍照路径。
- 改版目标以 `docs/party-recorder-redesign-requirements.md` 为准：用户必须能在三步内创建聚会房间并拍第一张照片。
- 技术域名边界不随产品名变化：酒桌判官/聚会记录师的小程序后端、API、后台部署和验证目标仍只能是 `api.pomer.cn`，不得触碰 `pomer.cn` 公司官网。
- 2026-06-16 用户已明确授权：线上服务器除公司官网项目外，可作为酒桌判官/聚会记录师测试服务器使用。该授权覆盖 `api.pomer.cn` 对应的后端、API、后台、测试数据、部署、重启和清理操作；不覆盖 `pomer.cn` 官网项目、`pomer` 官网 PM2 服务、官网 Nginx server block、官网目录或无关项目。执行线上测试写入、部署、重启、后台 action 或清理时，仍必须记录目标服务、命令、证据、备份/回滚或清理方式，避免无法追溯。

## 进度节点修改边界

- 前端、后端、后台、测试、运维、风控、UI/UX、运营等成员只能修改自己负责的任务节点、交付记录和证据说明。
- 总进度节点、跨角色结论、里程碑状态、PM 审核结论只能由项目经理/Codex PM 在验证后汇总更新。
- Codex PM 只负责监督文档、核验证据、汇总总进度和退回问题，不直接承担前端、后端、后台等角色的业务源码实现。
- 任何成员不得直接把他人任务、跨角色任务或总进度改为“已完成”。
- 如果任务依赖他人协作，只能在自己的节点标记“待联调 / 阻塞 / 待复核”，并写明依赖的任务编号、角色和缺少的证据。
- 避免多人重复修改同一文件。角色进度优先写入对应角色计划；总进度只写入 `docs/gameplay-moments-progress-tracker.md`，由 PM 统一维护。
- UI/UX 成员处理界面、交互、视觉、截图或设计验收任务时，必须先自动选择合适的 SKILL 配合使用，并在自己的计划或评审记录里写明选择依据、使用边界和证据缺口。
- UI/UX 成员可在明确授权后直接生成并落地视觉资产，包括设计图、切图、按钮状态、空态、背景、贴纸、分享图底图等；资产应写入 UI/UX 计划或资产清单，标明文件路径、尺寸、用途、压缩要求和前端接入方式。UI/UX 不得因此修改业务源码、PM 总台账或测试验收结论。
- Codex PM 每次汇报或审核后，必须明确“接下来谁该做什么”：至少写清责任角色、任务编号、下一步动作、依赖证据或验收方式。不能只给状态总结。
- 每次新增人员、调整职责或新增跨角色协作内容时，Codex PM 必须同步更新 `docs/gameplay-moments-team-announcements.md`，把加入人员、职责边界、协作对象、不得修改的文件边界和下一步动作告知所有角色；每日 23:00 自动化也必须检查是否存在未公告的人员或职责变更。
- 当前开发推进阶段的验收方式以微信开发者工具右侧预览框为准：能在预览框内按真机交互点击、切换页面、完成核心路径，并结合开发者工具 Console / Network / storage 检查无阻塞错误，即可作为本阶段通过依据；暂不要求 iPhone 真机截图、录屏、设备窗口或体验版二维码，避免影响开发进度。
- 真机测试截图需求暂停作为开发推进阻塞。除非出现开发者工具预览框无法复现、权限/保存/扫码等只能真机判断的 P0 问题，测试/PM/UI/UX 不得反复向用户索要截图录屏。后续若进入正式发布准出，再由 PM 另开最终真机采集任务并一次性列出所需材料。
- 当前 UI/UX 审核重点转为界面交互体验：三步内创建并拍照、页面权重、元素不越界、列表不厚重、按钮可点可达、反馈清晰、旧品牌/旧玩法权重消退。开发者工具预览框通过时，测试/UI/UX 可写“预览框阶段通过”；不得写成“正式真机发布通过”。

## 协作前置核查

遇到依赖其他角色的工作时，先读取本迭代相关文档，确认协作项是否已有代码、接口、文档和验证证据。只有计划没有证据时，不得标记完成，不得伪造 mock 状态或假接接口。

## Windows 与 PowerShell 编码安全

在 Windows 上编辑源码、文档或含中文内容的配置时，优先使用最新稳定 PowerShell 7 运行时；当前项目约定版本为 `pwsh 7.6.3`。

- 修改前可用 `$PSVersionTable.PSVersion.ToString()` 确认版本，期望值为 `7.6.3`。
- 不要依赖 Windows PowerShell 5.1 处理 UTF-8 / 中文源码写入。
- 不要通过 PowerShell stdin 管道写入中文源码，避免控制台编码把中文变成问号。
- 手工小范围修改优先使用 `apply_patch`。
- 脚本写入非 ASCII 内容时，必须确保输入文件本身是 UTF-8，或使用完整 Unicode escape，不能使用不完整 `\u` 序列。
- 修改后优先运行 `npm.cmd run check:encoding` 或项目现有轻量编码检查。

## 流程阻塞与收口规则

- 角色线程遇到同一任务连续两次阻塞、或 PM 发出“收口指令”后，必须停止扩大实现范围，只输出最终状态、失败命令、错误原文、是否有残留、下一步责任人和所需证据。
- 任何固定联调数据任务必须先满足 actual manifest 准入：`docs/runtime/int-data-001-manifest.json` 存在、JSON 可解析、包含 session/profile/moment/task/report/nomination/payout 等实际 ID 或明确 warnings/skipped、并有 cleanup/残留扫描证据。未满足前，测试、前端、后台、UGC、UI/UX 均只能标记待联调。
- PM 线程工具不可用、角色线程未最终答复或证据不完整时，PM 只能做只读核查和总台账“未闭环”记录，不得代替角色执行 create/cleanup/线上写入，也不得把任务标记完成。
- 后端/API、接口联调、测试等角色交接时，必须给出可复跑命令、执行结果、失败原文、数据残留状态和下一步命令；只有“脚本已改”或“计划已写”不能作为交接完成证据。
- `api.pomer.cn` 已按测试服务器授权处理：角色可在各自职责内执行必要的线上测试写入、后台写操作、部署重启或清理数据，不再逐项等待单独授权；但必须先确认目标属于酒桌判官/聚会记录师服务，并记录目标环境、备份/回滚或清理方式、执行命令和验证证据。`pomer.cn` 官网项目和无关服务仍不得触碰。
