# 聚会记录师 PM 活动台账

启动时间：2026-06-22

旧工作 Markdown 备份目录：

`C:\Users\Administrator\Desktop\jiuzhuopanguan-md-backup-20260622-002950`

当前规则：

- 只在本文件记录后续 PM 派发、验收、阻塞和收口。
- 旧工作长文档已清空，避免继续消耗上下文。
- `AGENTS.md`、`.agents/skills/**/*.md`、`DEPLOY.md`、`PRD.md` 保留。
- 产品面向用户统一叫“聚会记录师”。
- 技术边界只限 `api.pomer.cn` / jiuzhuopanguan，禁止触碰 `pomer.cn` 官网。
- PM 不直接改业务源码，只负责监督、派发、验收和总控记录。

## 当前待接续

- 2026-06-22 已按用户确认“所有线程已结束”归档旧 9 个角色线程。
- 已启用可用的新 PM 与 9 个角色待命线程，标题后缀为 `待命2`。
- 最新 P0：个人中心已结束统计为 0，需后端/API、接口联调、后台、前端、测试按 `008FA-008FE` 对账。

## 当前可用待命线程

| 角色 | 线程 ID | 标题 |
| --- | --- | --- |
| PM 总控 | `019eebc5-ea32-7c11-bfdc-4a496edbff08` | PM总控-聚会记录师新组-待命2 |
| 前端 | `019eebc6-99f6-71e2-83c4-e0c9d2afd6e2` | 前端负责人-聚会记录师新组-待命2 |
| UI/UX | `019eebc6-b453-74a2-b717-b3c54897d395` | UIUX负责人-聚会记录师新组-待命2 |
| 测试验收 | `019eebc6-d497-7d80-9c6b-53254355fa69` | 测试验收负责人-聚会记录师新组-待命2 |
| 后端/API | `019eebc6-f69d-73b2-a5d3-7f8e581283ac` | 后端API负责人-聚会记录师新组-待命2 |
| 接口联调 | `019eebc7-16dd-7cf0-bf03-1114f6a4881e` | 接口联调负责人-聚会记录师新组-待命2 |
| 后台管理 | `019eebc7-3b42-7290-8abc-6bc533117144` | 后台管理负责人-聚会记录师新组-待命2 |
| UGC 风控 | `019eebc7-5b17-72e3-bfaa-6820cc3e1dfb` | UGC风控负责人-聚会记录师新组-待命2 |
| DBA/运维 | `019eebc7-7f70-7842-8899-826d6ef7bbaa` | DBA运维负责人-聚会记录师新组-待命2 |
| 数据运营 | `019eebc7-a103-7012-ad85-ef479a08e718` | 数据运营负责人-聚会记录师新组-待命2 |

状态：2026-06-22 复查均为 `idle`。

## 已归档待命失败批次

| 角色 | 线程 ID | 标题 |
| --- | --- | --- |
| PM 总控 | `019eeb0c-e63c-7ae1-bba6-f2d17e1a0a19` | PM总控-聚会记录师新组-待命 |
| 前端 | `019eeb0d-01ac-7900-ad7c-c9477763c3ae` | 前端负责人-聚会记录师新组-待命 |
| UI/UX | `019eeb0d-23a1-7e21-a537-c3023a76d3e6` | UIUX负责人-聚会记录师新组-待命 |
| 测试验收 | `019eeb0d-3f91-7841-949d-a0fda57b7f5a` | 测试验收负责人-聚会记录师新组-待命 |
| 后端/API | `019eeb0d-5a61-7712-a9b6-731c3a22b2b0` | 后端API负责人-聚会记录师新组-待命 |
| 接口联调 | `019eeb0d-766b-76f2-9f9d-b5afbd9016dd` | 接口联调负责人-聚会记录师新组-待命 |
| 后台管理 | `019eeb0d-9686-7180-8aea-223dea19c045` | 后台管理负责人-聚会记录师新组-待命 |
| UGC 风控 | `019eeb0d-b1cb-70f2-9e5c-834cc6c4f135` | UGC风控负责人-聚会记录师新组-待命 |
| DBA/运维 | `019eeb0d-d046-75b1-ad4c-5b802843f05f` | DBA运维负责人-聚会记录师新组-待命 |
| 数据运营 | `019eeb0d-ec13-7890-93a7-ee5c3c248d89` | 数据运营负责人-聚会记录师新组-待命 |

原因：创建后状态均为 `systemError`，不可作为可用线程继续派发。

## 已归档旧角色线程

| 角色 | 旧线程 ID |
| --- | --- |
| 前端 | `019ee288-d810-7f32-b663-3eb3d2d06be6` |
| UI/UX | `019ee288-f2ad-7ec0-8af4-6991f902cb05` |
| 测试验收 | `019ee289-17ce-75c2-a733-316673c2dd31` |
| 后端/API | `019ee289-3f2d-7f82-adf2-caf4f48595db` |
| 接口联调 | `019ee289-6a4f-7273-b925-c38cee9bd5f2` |
| 后台管理 | `019ee289-8f67-7f90-b6d9-4397619ca214` |
| UGC 风控 | `019ee289-b189-7670-99b0-16791ee3efe4` |
| DBA/运维 | `019ee289-dc57-7682-ad63-405380d515ea` |
| 数据运营 | `019ee289-fe30-7670-aae6-f35b988ad6dd` |

## 已归档异常重启批次

| 角色 | 异常线程 ID |
| --- | --- |
| PM 总控 | `019eeb09-8fae-7c21-ae9a-e46fc74ddb8a` |
| 前端 | `019eeb07-329b-7623-823e-c87196b04607` |
| UI/UX | `019eeb07-ed11-79e1-bfb6-d7968f2cd72f` |
| 测试验收 | `019eeb08-091f-7370-b110-acda57126b18` |
| 后端/API | `019eeb08-2e3a-7a22-aaee-b59ee24526b5` |
| 接口联调 | `019eeb08-536a-72b2-b01d-56952e3e3e34` |
| 后台管理 | `019eeb08-7893-7512-a94b-2ab3868e4d4a` |
| UGC 风控 | `019eeb08-9da4-7fc3-ba94-b7774f01cf43` |
| DBA/运维 | `019eeb08-c17b-7af0-892d-2c90a5ef8a6c` |
| 数据运营 | `019eeb08-e9bb-7c21-99f2-b038968b94ab` |

## 残留说明

- 异常重启批次中的后台管理线程产生了后台代码改动，文件为 `backend/data/admin.js`、`backend/public/admin/static/heatwave-ops/app.js`、`backend/public/admin/static/heatwave-ops/sessions.html`。
- 2026-06-22 PM 已审核 diff：改动方向与“已结束记录后台可查”P0 一致，`node --check` 通过；当前暂时保留，等待 008FC/008FE 联调验收，不回滚。

## 2026-06-22 当前处理

- 已将根目录 `PRD.md` 更新为当前项目适用的“聚会记录师 PRD”。
- 当前 P0 继续聚焦：个人中心已结束统计为 0，以及已结束记录后台可查。
- 已向后端/API、接口联调、后台管理、前端、测试验收待命2线程派发 `008FA-008FE`。

## 2026-06-22 PRD 与 P0 收口状态

- `PRD.md` 已更新为当前项目适用版本：以“聚会记录师”为用户侧产品名，主链路为创建聚会、邀请、首拍、记录/相册/分享/账本、结束聚会、个人中心归档、后台管理。
- `008FA` 后端/API：已完成本地合同与 smoke 证明。`/user/session-moment-summaries` 可返回已结束态字段，已结束摘要包含 `state/status/stateText=已结束`、`endedAt`、`canResume=false`。
- `008FC` 后台管理：已完成本地后台数据层验证与最小修正。后台“聚会管理”可查进行中/已结束、`endedAt`、成员、report/brief/share task 关联；本地样本 `total=13`、`ongoing=7`、`ended=6`。
- `008FD` 前端：已完成消费链路核查与最小修复。个人中心不再把 `/user/session-moment-summaries` 401、token 失效或网络失败伪装成 `0 / 0 / 0`，失败时统计显示 `--`。
- `008FB` 接口联调：本地数据层对账通过；线上用户接口层仍阻塞。现有 `INT-DATA-001` token 全部 401，不能证明当前复现用户在 `api.pomer.cn` 的 summary 已返回非 0 已结束摘要。
- `008FE` 测试验收：当前仍应保持 waiting/blocked。此前 008FE 记录写入时尚未看到 008FD 回包，需测试负责人基于新 008FD/008FC/008FB 记录重新复测。

### 当前未闭环证据

- 缺当前微信开发者工具复现环境中的有效 `jzp-user-token`，只需用于接口联调复跑，不得在文档中泄露完整 token。
- 缺同一用户、同一 session 的线上 `/api/v1/user/session-moment-summaries` 与 `/api/v1/reports/history?mode=all` 对账结果。
- 缺测试验收在开发者工具预览框内重新确认：个人中心统计、相册已结束分类、已结束不进入继续记录入口、Console/Network 无新增阻塞 401/404/5xx。

### 接下来谁做什么

- 接口联调 `008FB`：等待前端或测试提供当前有效 `jzp-user-token` 后，只读复跑线上 summary/history，并回报 token 后 8 位、HTTP 状态、`total/ongoing/ended`、样本 `sessionId/profileId`。
- 测试验收 `008FE`：重新读取 `008FA/008FB/008FC/008FD` 交付记录；在拿到有效 token 对账前保持 blocked，拿到后执行开发者工具预览框复测。
- 前端 `008FD`：如测试仍看到已结束为 0，先确认 Console 是否有 `[me] failed to load session moment summaries`；若有，交给接口联调/后端处理登录态或用户接口；若接口成功但 UI 错，再回前端查渲染。
- 后端/API `008FA`：若有效 token 下 summary 仍为 0，查线上部署版本、profileId 成员映射和 `listManagedReports()` 过滤。

### 已重新派发

- 2026-06-22：已向测试验收线程重新派发 `008FE`，要求基于 `008FA/008FB/008FC/008FD` 最新证据复测；优先读取微信开发者工具当前 storage 获取有效 `jzp-user-token`，文档只允许写 token 后 8 位。若无有效 token 或接口仍 401，只能写 blocked，不得写通过。
- 2026-06-22：测试验收 `008FE` 已按收口指令更新为 blocked。直接原因：微信开发者工具自动化端口 `127.0.0.1:9420` 未开放，未取得可确认属于当前登录态的有效 `jzp-user-token`；不得写通过、上线或真机准出。

## 下一步规则

- 后续从 `PM总控-聚会记录师新组-待命2` 继续派发。
- 不向已归档或 `systemError` 线程派发。
- 如需旧证据，从桌面备份目录按路径查找，不再通读旧长文档。

## 2026-06-22 基建与安全加固派发

用户新增并行目标：

1. 把 `app_store` 中的高频业务对象拆成实体表。
2. 接入正式对象存储或 CDN，替代本地 `/uploads/`。
3. 增加后台密码重置、登录失败限制。
4. 给 Nginx、MySQL、PM2 增加备份和监控。

PM 只读复查：

- `pwsh -NoLogo -NoProfile -Command '$PSVersionTable.PSVersion.ToString()'` 返回 `7.6.3`。
- `Test-NetConnection -ComputerName 127.0.0.1 -Port 9420` 返回 `TcpTestSucceeded=False`。用户已打开微信开发者工具窗口，但本机 9420 自动化端口仍未监听；`008FB/008FE` 继续缺有效当前登录态 `jzp-user-token`，不能写通过。

已派发：

- DBA/运维 `009`：实体表迁移、对象存储/CDN 运维边界、Nginx/MySQL/PM2 备份监控方案与证据，交付 `docs/runtime/pr-dba-ops-infra-hardening-009.md`。
- 后端/API `009`：`app_store` 到实体表读写切换、上传 provider/URL 合同、后台安全 API 支撑，交付 `docs/runtime/pr-backend-infra-hardening-009.md`。
- 后台管理 `009`：后台密码重置、登录失败限制、锁定提示和操作日志，交付 `docs/runtime/pr-admin-security-hardening-009.md`。
- 前端 `009`：9420 端口协助确认、对象存储/CDN URL 兼容点审计，交付 `docs/runtime/pr-fe-upload-cdn-compat-009.md`。
- 接口联调 `009`：继续 `008FB` token/线上对账；同时准备实体表、上传 CDN、安全接口、备份监控联调矩阵，交付 `docs/runtime/pr-int-infra-hardening-009.md`。
- 测试验收 `009`：继续 `008FE` blocked/复测条件；同时准备基建与安全验收清单，交付 `docs/runtime/pr-qa-infra-hardening-009.md`。

验收边界：

- 没有实际 schema/代码/配置/命令输出/恢复演练/线上或本地可复跑证据的项，只能标“待联调 / 阻塞 / 待验收”。
- `pomer.cn` 公司官网不在本次目标内；任何服务器级操作只能面向 `api.pomer.cn` / jiuzhuopanguan，并记录目标服务、命令、备份/回滚和证据。
- 对象存储/CDN 未明确 provider 与凭证前，不得在前后端假接死 URL；旧 `/uploads/` 迁移必须保留兼容或有明确回滚。

## 2026-06-22 CDN/OSS 配置验收

用户已在阿里云完成：

- OSS Bucket：`pomer-party-recorder-prod`
- 地域：华北 2（北京）
- OSS Endpoint：`oss-cn-beijing.aliyuncs.com`
- 源站域名：`pomer-party-recorder-prod.oss-cn-beijing.aliyuncs.com`
- CDN 域名：`cdn.pomer.cn`
- CNAME：`cdn.pomer.cn.w.kunlunaq.com`
- HTTPS 证书：已部署到 `cdn.pomer.cn`
- HTTP 强制跳 HTTPS：已生效
- CDN 私有 OSS Bucket 回源：已开启，同账号回源
- Bucket 读写权限：保持私有

PM 本机只读验收命令与结果：

- `Resolve-DnsName cdn.pomer.cn -Type CNAME`：返回 `cdn.pomer.cn.w.kunlunaq.com`。
- `curl.exe -I http://cdn.pomer.cn/cdn-check.png`：返回 `301 Moved Permanently`，`Location: https://cdn.pomer.cn/cdn-check.png`。
- `curl.exe -I https://cdn.pomer.cn/cdn-check.png`：返回 `HTTP/1.1 200 OK`，`Content-Type: image/png`，`Content-Length: 45947`，`x-oss-cdn-auth: success`，`Accept-Ranges: bytes`。
- `curl.exe -s https://cdn.pomer.cn/cdn-check.png --output NUL --write-out ...`：返回 `http_code=200 content_type=image/png size=45947`。

结论：

- CDN/DNS/HTTPS/私有 OSS 回源/真实图片访问链路已通过基础验收。
- 根路径 `https://cdn.pomer.cn/` 返回 403 属于私有 Bucket 根目录不可匿名访问，不作为阻塞。
- 后端/API `009` 下一步可进入 OSS 上传 provider 接入：上传目标 Bucket `pomer-party-recorder-prod`，对象 key 建议按 `moments/{sessionId}/...`、`admin/{folder}/...`、`share-tasks/...` 分区；返回给前端的公开访问 URL 使用 `https://cdn.pomer.cn/{objectKey}`，同时保留旧 `/uploads/` 兼容和回滚。
- 接口联调 `009` 需要新增验收：上传一张真实图片到 OSS 后，校验接口返回 URL、CDN 200、Content-Type、缓存头、旧数据兼容、失败回滚。

## 2026-06-22 资源包购买与角色进展复核

用户已购买：

- 阿里云 CDN/全站加速/ESA 资源包
- 资源包类型：CDN/DCDN 下行流量
- 下行流量：500GB
- 加速区域：全国通用（中国内地）

角色进展复核：

- 后端/API `009`：已落地 OSS/CDN provider 接入。新增 `backend/data/object-storage.js`，更新 `backend/data/assets.js`、`backend/data/moments.js`、`backend/package.json`、`backend/package-lock.json`；默认 `UPLOAD_PROVIDER=local`，配置 `UPLOAD_PROVIDER=oss` 后上传到 `pomer-party-recorder-prod` 并返回 `https://cdn.pomer.cn/{objectKey}`；保留本地镜像和旧 `/uploads/` 回滚。已通过 `node --check`、`npm.cmd run check:encoding`、local provider smoke、CDN 只读读取 smoke。未完成真实 OSS `putObject()`，因为本机没有生产 OSS AccessKey。
- DBA/运维 `009`：已把 CDN/OSS 状态更新为“基础链路已验收，待后端接入 provider 和接口联调”，补充证书续期、探针、监控告警建议；仍需服务器环境变量、备份监控落地和恢复演练证据。
- 后台管理 `009`：已实现后台密码重置、登录失败计数、5 次失败锁定 15 分钟、登录/失败/锁定/重置操作日志和系统权限页入口。已通过语法、编码和静态合同检查；未执行真实 5 次失败或真实密码重置，避免污染当前管理员数据。
- 前端 `009`：已启动并确认微信开发者工具自动化端口 `9420` 可用，读取当前 storage token 后 8 位 `a9160666`。只读线上接口发现 `/reports/history?mode=all` 有已结束样本，但 `/user/session-moment-summaries` 同 session 的 `state/status/stateText/endedAt/canResume` 为空，前端只能判定 ongoing；已记录 CDN URL 兼容点。
- 接口联调 `009`：已更新 CDN 基础链路通过和后端上传接口待联调矩阵；尚未基于后端 OSS provider 做真实上传接口复测。
- 测试验收 `009`：已更新为“CDN/OSS 基础链路通过，待后端上传接口接入后复测业务链路”；明确不能只用手工上传图片冒充业务通过。

当前 PM 判断：

- CDN/OSS 基础设施已可用，资源包已购买。
- 仓库后端代码已具备 provider 接入雏形，但生产环境尚未启用；需要用户提供或在服务器配置 RAM AccessKey 环境变量后，才能做真实 OSS 上传验证、部署和接口联调。
- P0 `008FE` 原端口阻塞已解除，但发现新的后端/API 字段问题：当前有效 token 下 summary 字段缺失，history 有已结束状态。需后端/API `008FA` 重新查线上 summary 字段映射或部署版本；接口联调再复跑 summary/history。

## 2026-06-22 线上最小部署与 P0 回归

执行边界：

- 仅面向 `api.pomer.cn` 对应的 `jiuzhuopanguan-backend` 服务。
- 同机 PM2 另有 `pomer` 官网服务，本次未重启、未改动、未代理、未部署官网。
- 因本地和服务器工作区均有大量历史未提交改动，本次不执行 `git pull`，采用文件级备份和最小文件同步。

线上只读确认：

- PM2 目标服务：`jiuzhuopanguan-backend`，脚本路径 `/www/wwwroot/jiuzhuopanguan-git/backend/server.js`，端口 `3010`，状态 online。
- 官网服务：PM2 `pomer` 独立在线，未纳入本次操作。
- 线上 MySQL：`npm run mysql:test` 返回 `ok=true`，表 `app_store`，`total=5`。
- 线上旧 `backend/data/moments.js` 缺少 `getSummaryStateFields`、`state/status/stateText/endedAt/canResume` summary 字段映射，是 `008FE` 已结束统计为 0 的线上直接原因。

备份与部署：

- 线上备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-deploy-20260622053325`。
- PM2 重启前状态备份：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/pm2/pm2-jlist-before-restart-20260622053325.json`。
- 同步文件：
  - `backend/data/moments.js`
  - `backend/data/assets.js`
  - `backend/data/object-storage.js`
  - `backend/package.json`
  - `backend/package-lock.json`
- 执行 `npm install`，新增 OSS 依赖，结果 `found 0 vulnerabilities`。
- 重启命令：`pm2 restart jiuzhuopanguan-backend --update-env`。

部署后验证：

- `node --check server.js`
- `node --check data/moments.js`
- `node --check data/assets.js`
- `node --check data/object-storage.js`
- `npm run mysql:test`：`ok=true`。
- `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- `https://api.pomer.cn/api/v1/config/templates`：HTTP 200。
- `https://api.pomer.cn/admin/login`：HTTP 200。
- `https://api.pomer.cn/admin`：HTTP 302 跳登录，符合未登录入口行为。
- `https://cdn.pomer.cn/cdn-check.png`：HTTP 200，`Content-Type=image/png`，`x-oss-cdn-auth=success`。
- 线上 `object-storage` 当前 provider 为 `local`，示例 URL 仍为 `/uploads/moments/codex-check.txt`；未启用 OSS，避免缺生产 AccessKey 导致业务上传失败。

P0 用户态回归：

- 微信开发者工具当前 storage token 后 8 位：`a9160666`。
- `GET https://api.pomer.cn/api/v1/user/session-moment-summaries`：HTTP 200，`total=22`，`ended=16`。
- `GET https://api.pomer.cn/api/v1/reports/history?mode=all`：HTTP 200，`total=22`，`ended=16`。
- 样本 `session-1781756002741-4efdab`：summary 返回 `state/status/stateText=已结束`，`endedAt=2026-06-18T04:13:22.741Z`，`canResume=false`。

当前 PM 判断：

- `008FE` 的线上 summary/history 已结束统计一致性已从接口层闭环，下一步测试验收可在微信开发者工具预览框复测个人中心统计、相册已结束分类和继续记录入口。
- OSS/CDN 代码已部署但未切流；下一步由 DBA/运维在服务器为 `jiuzhuopanguan-backend` 注入专用 RAM AccessKey 和 `UPLOAD_PROVIDER=oss` 等变量，再由接口联调做真实上传对象验证。
- 后台密码重置和登录失败限制仍停留在本地实现证据；本次未部署 `backend/server.js`、`backend/data/admin.js` 和后台静态页安全改动，避免混入其他未复核接口变更。下一步后台管理需提供更小上线包或 PM 另行做最小 diff 复核后再部署。

## 2026-06-22 后台安全最小部署

执行边界：

- 仅面向 `api.pomer.cn` 对应的 `jiuzhuopanguan-backend` 服务。
- 未改动、未重启 PM2 `pomer` 官网服务。
- 未直接上传本地整份 `backend/server.js`；先拉取服务器当前 `server.js`，只加后台安全所需最小补丁，避免带入本地其他未复核接口变更。

备份与部署：

- 线上备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-admin-security-20260622053745`。
- PM2 重启前状态备份：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/pm2/pm2-jlist-before-admin-security-restart-20260622053745.json`。
- 同步/补丁文件：
  - `server.js`：仅增加 `resetAdminPassword` 导入、登录 IP/UA 参数、`POST /api/v1/admin/users/:id/reset-password` 路由。
  - `backend/data/admin.js`：上线登录失败计数、5 次失败锁定 15 分钟、密码重置、操作日志字段。
  - `backend/public/admin/static/heatwave-ops/app.js`
  - `backend/public/admin/static/heatwave-ops/login.html`
  - `backend/public/admin/static/heatwave-ops/system-permissions.html`
- 重启命令：`pm2 restart jiuzhuopanguan-backend --update-env`。

部署后验证：

- 本地补丁副本：`node --check server.remote.js` 通过。
- 服务器：`node --check server.js`、`node --check data/admin.js`、`node --check public/admin/static/heatwave-ops/app.js` 通过。
- 服务器：`npm run mysql:test` 返回 `ok=true`。
- 服务器缺 `scripts/check-admin-ui-contract.js`，因此未在服务器跑该静态合同脚本；本地角色线程此前已跑通过。
- PM2：`jiuzhuopanguan-backend` online，PM2 `pomer` 官网服务仍 online 且未重启。
- 公网：`GET https://api.pomer.cn/admin/login` 返回 HTTP 200。
- 公网：未登录 `POST https://api.pomer.cn/api/v1/admin/users/admin-super/reset-password` 返回 HTTP 401，证明重置接口已上线并受后台会话保护；未执行真实管理员密码重置，避免污染生产账号。
- P0 回归未回退：token 后 8 位 `a9160666` 下 summary/history 仍均为 `total=22 / ended=16`。

当前 PM 判断：

- 后台密码重置和登录失败限制已完成线上最小部署；还缺“使用测试管理员账号执行真实 5 次失败锁定、超管重置密码、旧会话失效/保留策略”的受控写入验收。
- 下一步后台管理 `009` 提供测试账号或可回滚账号，测试验收 `009` 执行后台登录失败限制与重置密码 E2E，不得使用生产主账号做破坏性验证。

## 2026-06-22 继续推进：运维备份、后台安全 E2E、实体表同步

### OSS 切流条件核查

线上 `.env` 核查结果：

- 当前 `object-storage` provider：`local`。
- `UPLOAD_PROVIDER`、`UPLOAD_BUCKET`、`UPLOAD_REGION`、`UPLOAD_PUBLIC_BASE_URL`、`OSS_SOURCE_HOST` 均未配置。
- `OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`UPLOAD_ACCESS_KEY_ID`、`UPLOAD_SECRET_ACCESS_KEY` 均未配置。

当前判断：

- 不能切 `UPLOAD_PROVIDER=oss`，否则业务上传会因缺 OSS 凭证失败。
- OSS/CDN 基础设施已可用，但真实上传联调必须等待 DBA/运维在服务器注入专用 RAM AccessKey 或等效角色授权。

下一步：

- DBA/运维 `009`：配置仅限 `pomer-party-recorder-prod/admin/*`、`pomer-party-recorder-prod/moments/*` 的专用 RAM 凭证，并只注入 `jiuzhuopanguan-backend` 环境，不得写入 `pomer` 官网服务。
- 接口联调 `009`：凭证注入并重启后，再执行真实上传，验收返回 URL 是否为 `https://cdn.pomer.cn/...`。

### Nginx/MySQL/PM2 备份与恢复演练

新增脚本：

- `deploy/ops/ops-009-backup-drill.sh`

线上执行结果：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-20260622103703`。
- Nginx：备份 `api.pomer.cn` 相关配置 2 份；`nginx -t` 成功。
- MySQL：生成全库 dump：`/www/backup/jiuzhuopanguan/ops-009-20260622103703/mysql/jiuzhuopanguan.20260622103703.sql.gz`。
- MySQL 恢复演练：因应用账号无建库权限，改用同库临时表恢复 `app_store`；`restored_app_store_rows=5`；临时表已删除，复查 `restore_temp_tables=0`。
- PM2：导出 `pm2-jlist.json`、`describe-jiuzhuopanguan-backend.txt`、最近 120 行日志。
- 健康检查：
  - `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
  - `https://api.pomer.cn/admin/login`：HTTP 200。
  - `https://cdn.pomer.cn/cdn-check.png`：HTTP 200，`Content-Type=image/png`，`x-oss-cdn-auth=success`。

当前判断：

- Nginx/MySQL/PM2 备份和恢复演练已有线上证据，可进入测试验收复核。
- 后续还需 DBA/运维补长期监控或定时任务落地，例如定时备份、日志轮转、健康探针告警；本轮完成的是一次性备份和恢复演练。

### 测试验收 008FE / 009

微信开发者工具预览框复测：

- 页面：`/pages/me/index`。
- 当前 token 后 8 位：`a9160666`。
- `sessionMomentClassificationTotals`：`total=22 / ongoing=6 / ended=16`。
- 已结束样本 `session-1781756002741-4efdab`：`state/status/stateText=已结束`，`endedAt=2026-06-18T04:13:22.741Z`，`canResume=false`。

相册已结束分类复测：

- 页面：`/pages/album/index?mode=ended`。
- `mode=ended`，`pageTitle=已结束`。
- `items=16`，`endedItems=16`，`ongoingItems=0`，`endedCount=16`，`loading=false`。
- 列表项 `stateType=ended`、`statusText=已结束`。

当前判断：

- `008FE` 的接口层和预览框阶段复测通过：个人中心已结束统计、相册已结束分类、已结束项不进入继续记录状态均有证据。
- 仍未执行正式真机发布准出；按项目规则，当前阶段不以真机截图作为阻塞。

### 后台安全 E2E

补充安全修复：

- `POST /api/v1/admin/users/:id/reset-password` 已补充 `role-super-admin` 权限限制；普通后台用户返回 403。

新增脚本：

- `backend/scripts/smoke-admin-security-e2e.js`

线上 E2E 证据：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-admin-security-20260622105800`。
- 使用临时账号，不使用生产主账号。
- 流程：
  - 临时普通管理员登录：HTTP 200。
  - 普通管理员重置目标账号密码：HTTP 403。
  - 目标账号连续 5 次错误登录：`401, 401, 401, 401, 423`。
  - 锁定期内目标账号正确密码登录：HTTP 423。
  - 临时超管登录：HTTP 200。
  - 临时超管重置目标账号密码：HTTP 200。
  - 目标账号旧密码登录：HTTP 401。
  - 目标账号新密码登录：HTTP 200。
- cleanup 后复查：临时账号 `0`、临时会话 `0`、`ops009-*` 测试日志 `0`。

当前判断：

- 后台密码重置、登录失败限制、锁定和超管权限边界已完成线上 E2E。
- PM2 `pomer` 官网服务未改动、未重启。

### 实体表拆分与同步验证

补充上线：

- 备份并同步新版 `backend/data/normalized-db.js`，使 moments 相关对象进入实体表同步。
- 备份并同步新版 `backend/scripts/sync-normalized-db.js`，初始化 moments store 后再同步。
- 备份并同步新版 `backend/sql/mysql-normalized-schema.sql`。
- 修复线上旧 schema 漂移：
  - `moment_nominations` 补 `client_nomination_id`、`profile_name`、`refunded_at`、`refund_reason`、`updated_at`。
  - `ranking_reward_rules` 补 `rank_start`、`rank_end`、`points`。
  - 创建缺失表 `ranking_reward_payouts`。
- schema 备份目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-schema-20260622110500`。

线上同步结果：

- `npm run db:sync-normalized` 最终成功，`schema statements=36`。
- 同步行数摘要：
  - `users=6`
  - `user_sessions=7`
  - `user_login_logs=67`
  - `wine_sessions=24`
  - `wine_session_members=30`
  - `wine_reports=14`
  - `moment_records=35`
  - `session_events=7`
  - `session_briefs=14`
  - `share_image_tasks=13`
  - `moment_reports=1`
  - `moment_nominations=2`
  - `ranking_reward_rules=7`
  - `ranking_reward_payouts=2`
  - `assets=64`
  - `admin_users=2`
  - `admin_operation_logs=24`

回归验证：

- PM2 `jiuzhuopanguan-backend` online。
- `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- 当前 token 后 8 位 `a9160666` 下 `/user/session-moment-summaries` 仍为 `total=22 / ended=16`。

当前判断：

- 高频对象实体表“同步落表”已完成并有线上行数证据。
- 仍不能标记“运行时已切读写实体表”：当前业务运行时仍以 `app_store`/store accessor 为主，实体表处于同步/对账阶段。下一步后端/API + DBA 需要逐步切读路径、写路径或双写策略，并为每个切换点补回滚方案和接口对账。

### 2026-06-22 收口复核：OSS 脚本就位与实体表审计

本地新增脚本校验：

- `backend/scripts/smoke-oss-upload-http.js`：`node --check` 通过。
- `backend/scripts/audit-normalized-tables.js`：`node --check` 通过。
- `deploy/ops/ops-009-enable-oss-env.sh`：`git diff --no-index --check` 通过，仅有 LF/CRLF 提示。
- `npm.cmd run check:encoding`：通过。

线上脚本落位：

- `backend/scripts/audit-normalized-tables.js` 已同步到 `/www/wwwroot/jiuzhuopanguan-git/backend/scripts/audit-normalized-tables.js`。
- `backend/scripts/smoke-oss-upload-http.js` 已同步到 `/www/wwwroot/jiuzhuopanguan-git/backend/scripts/smoke-oss-upload-http.js`。
- `deploy/ops/ops-009-enable-oss-env.sh` 已同步到 `/www/wwwroot/jiuzhuopanguan-git/backend/scripts/ops-009-enable-oss-env.sh`，`bash -n` 通过。

实体表审计证据：

- 证据文件：`/www/backup/jiuzhuopanguan/ops-009-normalized-audit-20260622111500/audit-normalized-tables.json`。
- 审计结果：`ok=true`，`missing=[]`，`emptyRequired=[]`。
- 关键行数：`users=6`、`wine_sessions=24`、`wine_session_members=30`、`wine_reports=14`、`moment_records=35`、`session_briefs=14`、`share_image_tasks=13`、`admin_users=2`、`assets=64`。

OSS/CDN 切流状态：

- 当前线上 `object-storage` 仍为 `provider=local`，样例 URL 仍是 `/uploads/moments/ops009-current-provider.txt`。
- `ops-009-enable-oss-env.sh` 在缺少 `OSS_ACCESS_KEY_ID` 时返回 `exit_code=2` 并安全退出，未修改 `.env`。
- 因服务器仍缺专用 OSS RAM AccessKey，暂不能把 `UPLOAD_PROVIDER` 切到 `oss`，否则真实上传会失败。

下一步责任：

- DBA/运维 `009`：在服务器为 `jiuzhuopanguan-backend` 注入专用 OSS RAM 凭证后执行 `scripts/ops-009-enable-oss-env.sh`，并保留脚本输出目录。
- 接口联调 `009`：OSS provider 变为 `oss` 后，用有效用户 token 执行 `node scripts/smoke-oss-upload-http.js`，验收上传接口返回 `https://cdn.pomer.cn/...` 且 CDN HEAD 为 200。
- 后端/API + DBA `009`：实体表已完成同步和审计，下一步只能推进“运行时读写切换 / 双写策略 / 回滚方案”，不得把当前同步阶段写成已完全替代 `app_store`。

### 2026-06-22 收口复核二：监控定时化与实体表影子对账

新增/更新脚本：

- `deploy/ops/ops-009-health-monitor.sh`：只读健康监控，覆盖 Nginx、MySQL、PM2、API、CDN。
- `deploy/ops/ops-009-install-cron.sh`：安装带项目标记的 crontab 块，安装前备份原 crontab。
- `backend/scripts/verify-normalized-shadow.js`：对比 `app_store` 源 JSON 与实体表行数。
- `backend/scripts/audit-normalized-tables.js`：补充覆盖 `points_task_claims`、`reward_redemptions`、`membership_orders`。
- `backend/package.json`：增加 `db:audit-normalized`、`db:verify-normalized-shadow` 复跑入口。
- `backend/data/normalized-db.js`：实体表同步从单纯 upsert 改为 upsert 后按主键清理陈旧影子行。

本地校验：

- `node --check backend/data/normalized-db.js`：通过。
- `node --check backend/scripts/verify-normalized-shadow.js`：通过。
- `node --check backend/scripts/audit-normalized-tables.js`：通过。
- `npm.cmd run check:encoding`：通过。
- 相关文件 `git diff --no-index --check`：通过，仅有 LF/CRLF 提示。

线上部署与备份：

- `normalized-db.js` 线上备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-normalized-prune-20260622115447`。
- 已同步脚本到 `/www/wwwroot/jiuzhuopanguan-git/backend/scripts/`。
- `package.json` 上传前已备份到线上 `backend/backups/package-json-before-ops009-scripts-*.json`。

实体表影子对账：

- 首次对账证据：`/www/backup/jiuzhuopanguan/ops-009-normalized-shadow-20260622113000/verify-normalized-shadow.json`。
- 首次结果：`ok=false`，36 张表中仅 `user_sessions` 不一致，实体表 `actualRows=8`，`app_store` 源数据 `expectedRows=7`。
- 修复动作：`npm run db:sync-normalized` 新逻辑清理 `user_sessions` 陈旧影子行 `1` 条。
- 修复后证据目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-prune-20260622115500`。
- 修复后结果：`ok=true`，`checkedTables=36`，`mismatches=[]`。

健康监控证据：

- 一次性健康监控证据目录：`/www/backup/jiuzhuopanguan/ops-009-health-20260622115522`。
- `status=0`。
- API：`/api/v1/config/home` HTTP 200，`/admin/login` HTTP 200。
- CDN：`https://cdn.pomer.cn/cdn-check.png` HTTP/2 200，`Content-Type=image/png`，`Content-Length=45947`，`x-oss-cdn-auth=success`。
- MySQL：`app_store_rows=5`，`normalized_wine_sessions=24`，`normalized_moment_records=35`，实体表审计 `ok=true`。
- PM2：`jiuzhuopanguan-backend` online，`script path=/www/wwwroot/jiuzhuopanguan-git/backend/server.js`，`unstable restarts=0`。

定时任务：

- crontab 安装前备份：`/www/backup/jiuzhuopanguan/crontab.before-ops009-20260622115935.txt`。
- 已安装项目标记块：`# BEGIN jiuzhuopanguan ops-009` 到 `# END jiuzhuopanguan ops-009`。
- 每 15 分钟执行健康监控：输出滚动目录 `/www/backup/jiuzhuopanguan/ops-009-health-latest`，日志 `/www/backup/jiuzhuopanguan/ops-009-cron-health.log`。
- 每日 03:20 执行 Nginx/MySQL/PM2 备份与恢复演练：日志 `/www/backup/jiuzhuopanguan/ops-009-cron-backup.log`。
- 已用同一 cron 命令手工执行健康监控一次，`ops-009-health-latest` 返回 `status=0`。

当前 PM 判断：

- Nginx/MySQL/PM2 已具备一次性备份恢复证据、健康监控脚本和 cron 定时落地证据。
- 实体表已达到“同步 + 陈旧行清理 + 36 表影子对账通过”阶段。
- 仍不能宣称实体表已完全替代 `app_store`：业务运行时读写仍未切到实体表，下一步必须由后端/API 做双写开关、读切换开关和接口级回滚方案。
- OSS/CDN 上传切流仍被服务器缺专用 OSS RAM AccessKey 阻塞；当前 provider 仍为 `local`。

下一步责任：

- DBA/运维 `009`：补 OSS RAM AccessKey 到 `jiuzhuopanguan-backend` 环境后执行 `scripts/ops-009-enable-oss-env.sh`。
- 接口联调 `009`：OSS 切流后执行 `node scripts/smoke-oss-upload-http.js`，验收返回 `https://cdn.pomer.cn/...`。
- 后端/API `009`：在实体表影子对账通过基础上补 `NORMALIZED_DB_WRITE` / `NORMALIZED_DB_READ` 灰度开关，先切只读低风险接口，再切用户主链路。
- 测试验收 `009`：复核 cron 监控证据目录、后台安全 E2E 证据、P0 个人中心/相册已结束链路证据，不得把 OSS 上传链路写成已完成。

### 2026-06-22 OSS RAM 注入与真实上传联调

OSS 环境注入：

- 用户已在阿里云 RAM 创建 `jzp-oss-uploader-prod`，并在服务器终端注入 `OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET`。
- 执行脚本：`./scripts/ops-009-enable-oss-env.sh`。
- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-oss-env-20260622123327`。
- `.env` 非密钥字段：
  - `UPLOAD_PROVIDER=oss`
  - `UPLOAD_BUCKET=pomer-party-recorder-prod`
  - `UPLOAD_REGION=oss-cn-beijing`
  - `UPLOAD_PUBLIC_BASE_URL=https://cdn.pomer.cn`
  - `OSS_SOURCE_HOST=pomer-party-recorder-prod.oss-cn-beijing.aliyuncs.com`
  - `UPLOAD_LOCAL_MIRROR=1`
- 密钥长度只读核查：`OSS_ACCESS_KEY_ID_len=24`，`OSS_ACCESS_KEY_SECRET_len=30`。
- `jiuzhuopanguan-backend` 已重启，PM2 online；`pomer` 官网服务未重启。
- 带 `load-env` 复核：`provider=oss`，样例 URL 为 `https://cdn.pomer.cn/moments/ops009-final-provider.txt`。

真实上传 E2E：

- 新增脚本：`backend/scripts/smoke-oss-upload-e2e.js`。
- 执行命令：`OPS009_STAMP=20260622125500 OPS009_RESTART_PM2=1 node scripts/smoke-oss-upload-e2e.js`。
- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-oss-upload-20260622125500`。
- 脚本流程：创建临时用户和临时 session，重启 `jiuzhuopanguan-backend` 让 token 生效，调用线上上传接口，清理临时用户/session/store 记录，再次重启目标后端。
- 上传结果：
  - `uploadHttp=201`
  - `provider=oss`
  - `objectKey=moments/session-1782103396743-3160eb/1782103398209-ops009-oss-smoke-1782103398099-67b747.webp`
  - `url=https://cdn.pomer.cn/moments/session-1782103396743-3160eb/1782103398209-ops009-oss-smoke-1782103398099-67b747.webp`
  - CDN HEAD：`status=200`，`Content-Type=image/webp`，`Content-Length=44`，`x-oss-cdn-auth=success`，`Cache-Control=public, max-age=2592000, immutable`。
- token 只记录后 8 位：`2efa4fb4`。

清理与残留：

- store 残留扫描：`profiles=0`、`userSessions=0`、`uploadedAssets=0`、`liveSessions=0`。
- 证据文件：`/www/backup/jiuzhuopanguan/ops-009-oss-upload-20260622125500/post-check/store-residual-scan.json`。
- OSS 测试对象删除失败：RAM 当前无 `oss:DeleteObject` 权限，返回 `AccessDeniedError`。
- 因此 CDN 上仍可能保留 1 个测试对象：`moments/session-1782103396743-3160eb/1782103398209-ops009-oss-smoke-1782103398099-67b747.webp`。该对象本身是 1x1 smoke 图片，非用户数据。

实体表与健康回归：

- 上传清理后首次影子对账发现 `admin_sessions actualRows=1 / expectedRows=0`，属于陈旧影子行。
- 已执行 `npm run db:sync-normalized` 清理 `admin_sessions` 陈旧行 `1` 条。
- 修复后证据：`/www/backup/jiuzhuopanguan/ops-009-oss-upload-20260622125500/post-sync/verify-normalized-shadow.json`。
- 修复后结果：`ok=true`，`checkedTables=36`，`mismatches=[]`。
- 健康监控证据：`/www/backup/jiuzhuopanguan/ops-009-health-20260622130000`，`status=0`。
- PM2：`jiuzhuopanguan-backend` online，`unstable restarts=0`；`pomer` 官网服务 online，`restarts=0`。

当前 PM 判断：

- OSS RAM 环境变量配置已完成。
- OSS provider 已切到 `oss`。
- 接口联调 `009` 的真实上传 CDN URL 已闭环：上传接口返回 `https://cdn.pomer.cn/...`，CDN HEAD 返回 200。
- 仍有一个权限缺口：当前 RAM 策略缺少 `oss:DeleteObject`，测试对象无法由脚本清理；若后续需要自动清理测试对象或业务删除对象，需要给同一 RAM 用户追加 `oss:DeleteObject` 到 `admin/*`、`moments/*` 范围。
- 实体表仍处于影子同步和对账通过阶段；运行时读写切换尚未完成。

下一步责任：

- DBA/运维 `009`：决定是否给 `jzp-oss-uploader-prod` 追加 `oss:DeleteObject`，至少用于 `moments/*`、`admin/*` 测试和业务删除清理。
- 接口联调 `009`：可把 OSS 上传 CDN URL 写为通过，但需注明测试对象删除受 RAM 权限限制。
- 测试验收 `009`：复核上传接口返回 CDN URL、CDN HEAD、store 残留扫描、PM2/健康监控证据。
- 后端/API `009`：继续推进实体表 `NORMALIZED_DB_READ` / `NORMALIZED_DB_WRITE` 灰度开关，不得把当前状态写成实体表已完全替代 `app_store`。

### 2026-06-22 实体表 reports/history 灰度切读

本轮新增/更新：

- `backend/data/normalized-read.js`：新增 `listManagedReportsFromNormalized()` 和 `NORMALIZED_DB_READ` 读开关判断。
- `backend/server.js`：`GET /api/v1/reports/history` 在 `NORMALIZED_DB_READ=reports` 时优先读实体表，异常时回退 `app_store` 原实现并打印 `[normalized-read] reports/history fallback`。
- `backend/sql/mysql-normalized-schema.sql`：补 `wine_sessions.image_url`；补 `wine_reports.name/image_url/player_count/share_rate`；`wine_sessions`、`wine_reports.created_at` 改为 `DATETIME(3)`。
- `backend/data/normalized-db.js`：补线上兼容迁移、实体表新字段映射、毫秒时间精度同步。
- `backend/scripts/verify-normalized-history-read.js`：对比 `app_store` 与实体表读取结果，成功时显式退出，避免 MySQL pool 挂住巡检进程。

本地校验：

- `node --check backend/data/normalized-db.js`：通过。
- `node --check backend/data/normalized-read.js`：通过。
- `node --check backend/scripts/verify-normalized-history-read.js`：通过。
- `npm.cmd run check:encoding`：通过。

线上部署与备份：

- 线上备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-normalized-history-read-20260622133500`。
- 线上同步命令：`npm run db:sync-normalized`。
- 同步证据：`/www/backup/jiuzhuopanguan/ops-009-normalized-history-read-20260622133500/sync-normalized.log`。
- 读等价验证：`/www/backup/jiuzhuopanguan/ops-009-normalized-history-read-20260622134000/verify-history-read.after-switch.json`。
- 验证结果：`ok=true`，`checkedCount=24`，`profileCount=6`，`mismatchCount=0`。
- `.env` 已启用 `NORMALIZED_DB_READ=reports`；启用前备份：`/www/backup/jiuzhuopanguan/env.before-normalized-read-reports-20260622134000`。
- 仅重启 `jiuzhuopanguan-backend`；`pomer` 官网 PM2 服务未重启。

线上接口验证：

- `GET https://api.pomer.cn/api/v1/reports/history?mode=all`：HTTP 200。
- token 仅记录后 8 位：`a9160666`。
- 返回数量：`count=22`。
- 样本：`id=session:session-1781756002741-4efdab`，`sessionId=session-1781756002741-4efdab`，`status=已结束`，`createdAt=2026-06-18T04:13:22.741Z`。
- 健康监控证据：`/www/backup/jiuzhuopanguan/ops-009-health-20260622130327`，`status=0`。
- 健康监控内 `config-home` HTTP 200；CDN `cdn-check.png` HTTP/2 200，`x-oss-cdn-auth=success`；实体表审计 `ok=true`。

临时故障与修复：

- 启用读开关后首次 API 验证返回 502，直接原因是线上缺少 `backend/data/clean-slate.js`，PM2 `jiuzhuopanguan-backend` 进入 `errored`。
- 修复动作：上传本地 `backend/data/clean-slate.js` 到线上 `backend/data/`，重启 `jiuzhuopanguan-backend`。
- 修复后 PM2：`jiuzhuopanguan-backend` online；`pomer` 官网服务 online 且未重启。
- PM2 错误日志中仍保留缺模块的历史记录；当前服务和 API 已恢复。

当前 PM 判断：

- `app_store` 高频对象拆实体表已从“影子同步”推进到 `reports/history` 低风险接口灰度读实体表。
- 不能宣称实体表已完全替代 `app_store`：当前只切了 `reports/history` 读路径，写路径和其他主链路仍依赖 `app_store`。
- OSS/CDN 已切为 `oss` provider，真实上传返回 `https://cdn.pomer.cn/...` 并经 CDN HEAD 验证通过。
- 后台密码重置、登录失败限制已有 E2E 证据；IP 白名单/二次校验本轮未实现，不得写完成。
- Nginx/MySQL/PM2 已有备份、恢复演练、健康监控和 cron 证据。

下一步责任：

- 后端/API `009`：继续推进 `NORMALIZED_DB_READ` 到个人中心 summary、相册/history 相关读接口；设计 `NORMALIZED_DB_WRITE` 双写或实体表主写策略，并给出回滚开关。
- DBA/运维 `009`：补 `oss:DeleteObject` 最小权限后清理 OSS smoke 对象；继续观察 `ops-009-health-latest` cron 产物。
- 测试验收 `009`：复测 `reports/history` 灰度读开关下个人中心已结束统计、相册已结束分类、已结束记录不进入继续记录入口；复核后台安全 E2E。
- 后台管理/安全 `009`：若用户仍要求 IP 白名单或二次校验，另起任务补实现和验收，不要并入“登录失败限制已完成”。

### 2026-06-22 summaries 实体表灰度读与验收复测

实体表 summaries 灰度读：

- 新增 `backend/data/normalized-read.js` 的 `listUserSessionMomentSummariesFromNormalized()`。
- `GET /api/v1/user/session-moment-summaries` 已接入 `NORMALIZED_DB_READ=summaries` 灰度读；异常时回退原 `app_store` 实现并打印 `[normalized-read] user/session-moment-summaries fallback`。
- 新增 `backend/scripts/verify-normalized-summary-read.js`，用于对比 `app_store` 与实体表 summary 输出。
- `backend/package.json` 新增 `db:verify-summary-read`。

本地校验：

- `node --check backend/data/normalized-read.js`：通过。
- `node --check backend/scripts/verify-normalized-summary-read.js`：通过。
- `node --check backend/scripts/smoke-admin-security-e2e.js`：通过。
- `node --check backend/server.js`：通过。
- `npm.cmd run check:encoding`：通过。

线上部署与切换：

- 备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-normalized-summary-read-20260622141000`。
- 切换前等价验证：`/www/backup/jiuzhuopanguan/ops-009-normalized-summary-read-20260622141000/verify-summary-read.before-switch.json`。
- 切换前结果：`ok=true`，`checkedCount=6`，`profileCount=6`，`mismatchCount=0`。
- `.env` 已从 `NORMALIZED_DB_READ=reports` 改为 `NORMALIZED_DB_READ=reports,summaries`。
- 切换前 env 备份：`/www/backup/jiuzhuopanguan/env.before-normalized-read-summaries-20260622141500`。
- 切换后验证目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-summary-read-20260622141500`。
- 切换后 `db:verify-summary-read` 与 `db:verify-history-read` 均通过。
- 仅重启 `jiuzhuopanguan-backend`；`pomer` 官网服务未重启。

线上接口复测：

- token 仅记录后 8 位：`a9160666`。
- `GET /api/v1/user/session-moment-summaries`：HTTP 200，`total=22`，`ended=16`。
- summary 样本：`sessionId=session-1781756002741-4efdab`，`state/status/stateText=已结束`，`endedAt=2026-06-18T04:13:22.741Z`，`canResume=false`。
- `GET /api/v1/reports/history?mode=all`：HTTP 200，`total=22`，`ended=16`。
- history 样本：同一 `sessionId=session-1781756002741-4efdab`，`status=已结束`，`createdAt=2026-06-18T04:13:22.741Z`。
- 健康监控：`/www/backup/jiuzhuopanguan/ops-009-health-20260622131953`，`status=0`。

微信开发者工具预览框复测：

- 自动化端口：`127.0.0.1:9420` 可连接。
- 已向开发者工具 storage 注入当前有效 token；对外只记录 token 后 8 位 `a9160666`。
- 个人中心 `/pages/me/index`：
  - `loggedIn=true`。
  - `sessionMomentClassificationTotals={ total: 22, ongoing: 6, ended: 16 }`。
  - Console：无新增错误。
- 相册已结束页 `/pages/album/index?mode=ended`：
  - 当前页面 `path=pages/album/index`，`query.mode=ended`。
  - `loading=false`。
  - 列表返回 16 条已结束记录，样本均为 `stateType=ended`、`statusText=已结束`。
  - Console：无新增错误。
- “已结束不进入继续记录入口”接口证据：summary 已结束样本 `canResume=false`、`canResumeMomentIds=[]`；相册已结束列表使用 `stateType=ended` 分类。

后台安全 E2E 复跑：

- 修复 `backend/scripts/smoke-admin-security-e2e.js`：补 `require('../load-env')`，确保线上命令行写入 MySQL `app_store` 而不是仅写本地文件镜像。
- 失败定位证据：未加载 `.env` 时 seed 后只读检查 `seededUsers=0`，PM2 无法读到测试账号。
- 通过证据目录：`/www/backup/jiuzhuopanguan/ops-009-admin-security-20260622143500`。
- 执行流程：`seed -> restart jiuzhuopanguan-backend -> verify -> cleanup -> restart jiuzhuopanguan-backend`。
- seed 后只读检查：`seededUsers=3`。
- verify 结果：
  - `contentLogin=200`
  - `contentResetTarget=403`
  - `targetWrongLogins=[401,401,401,401,423]`
  - `targetLockedLogin=423`
  - `superLogin=200`
  - `superResetTarget=200`
  - `targetOldPasswordLogin=401`
  - `targetNewPasswordLogin=200`
  - `ok=true`
- cleanup 结果：`adminUsers=0`、`sessions=0`、`operationLogs=0`。

当前 PM 判断：

- OSS/CDN、真实上传、Nginx/MySQL/PM2 备份监控、后台密码重置和登录失败限制已具备当前证据。
- P0 `008FE` 的接口层和开发者工具预览框阶段复测已闭环：个人中心统计、相册已结束分类、已结束不可继续记录均有当前证据。
- 实体表拆分已进一步推进：`reports/history` 与 `user/session-moment-summaries` 两条用户读路径已灰度读实体表，且对账通过。
- 仍不能标记完整目标完成：`NORMALIZED_DB_WRITE` 写路径/双写策略未完成；其他主链路读接口尚未全部切到实体表；OSS `oss:DeleteObject` 最小权限仍未补；IP 白名单/二次校验本轮仍未实现。

下一步责任：

- 后端/API `009`：继续补 `NORMALIZED_DB_WRITE` 双写或实体表主写方案，优先覆盖创建聚会、上传 moment、生成 brief/share task 的写路径。
- DBA/运维 `009`：为 `jzp-oss-uploader-prod` 追加 `oss:DeleteObject` 最小权限，并清理 smoke 对象。
- 测试验收 `009`：把本轮 `a9160666` token 下的个人中心/相册预览框结果写入验收报告，注明“预览框阶段通过，非正式真机发布通过”。
- 后台管理/安全 `009`：若继续要求 IP 白名单或二次校验，需另开实现与验收任务。

### 2026-06-22 NORMALIZED_DB_WRITE 影子写同步

目标：

- 在不把实体表改成主写源的前提下，补齐“业务写 `app_store` 后自动刷新实体表”的影子写同步能力。
- 当前仍保持 `app_store` 为主写，实体表作为灰度读和对账表；回滚只需移除 `NORMALIZED_DB_WRITE=shadow` 并重启 `jiuzhuopanguan-backend`。

本轮新增/更新：

- `backend/data/store-accessor.js`：在 MySQL `app_store` 持久化成功后，根据 `NORMALIZED_DB_WRITE` 触发 `syncNormalizedTables()`。
- 支持开关：`NORMALIZED_DB_WRITE=shadow`、`all`、或单独 store scope（如 `admin_store` / `admin`）。
- 默认不开启；开启后仅对 `admin_store`、`asset_manifest`、`content_store`、`moments_store`、`social_store` 生效。
- `backend/data/admin.js`：暴露 `flushAdminStore`，用于写后同步 smoke 精确等待持久化链路完成。
- `backend/scripts/smoke-normalized-shadow-write.js`：写入临时后台用户，验证实体表 `admin_users` 出现 1 行；删除后验证实体表清零。

本地校验：

- `node --check backend/data/store-accessor.js`：通过。
- `node --check backend/data/admin.js`：通过。
- `node --check backend/scripts/smoke-normalized-shadow-write.js`：通过。
- `npm.cmd run check:encoding`：通过。
- 相关文件 `git diff --no-index --check`：通过，仅有 LF/CRLF 提示。

线上部署与切换：

- 备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-normalized-write-shadow-20260622145500`。
- `.env` 已新增：`NORMALIZED_DB_WRITE=shadow`。
- 切换前 env 备份：`/www/backup/jiuzhuopanguan/env.before-normalized-write-shadow-20260622145500`。
- 仅重启 `jiuzhuopanguan-backend`；`pomer` 官网服务未重启。
- 当前线上关键 env：
  - `UPLOAD_PROVIDER=oss`
  - `NORMALIZED_DB_READ=reports,summaries`
  - `NORMALIZED_DB_WRITE=shadow`

写后同步 smoke：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-write-shadow-20260622145500`。
- `smoke-normalized-shadow-write.json`：
  - `normalizedWrite=shadow`
  - `insertedRows=1`
  - `remainingRows=0`
  - `ok=true`
- 说明：脚本没有手工执行 `db:sync-normalized`；通过 `store-accessor` 写后同步钩子完成实体表刷新。

回归验证：

- `verify-normalized-shadow.after-write.json`：`ok=true`，`checkedTables=36`，`mismatchCount=0`。
- `verify-history-read.after-write.json`：`ok=true`，`checkedCount=24`，`profileCount=6`，`mismatchCount=0`。
- `verify-summary-read.after-write.json`：`ok=true`，`checkedCount=6`，`profileCount=6`，`mismatchCount=0`。
- 健康监控：`/www/backup/jiuzhuopanguan/ops-009-health-20260622132802`，`status=0`。
- PM2：`jiuzhuopanguan-backend` online；`pomer` 官网服务 online 且未重启。

当前 PM 判断：

- 实体表拆分已从“手动同步 + 灰度读”推进到“灰度读 + 影子写同步”。
- 仍不能宣称实体表完全替代 `app_store`：当前主写源仍是 `app_store`，实体表还不是业务主写库；创建聚会、上传 moment、生成 brief/share task 等主链路尚未切为实体表主写。
- 当前可作为下一阶段切主写的前置证据：写后实体表自动同步、36 表对账、history/summary 读等价均通过。

下一步责任：

- 后端/API `009`：设计并实现实体表主写或双写切换的下一阶段开关，例如 `NORMALIZED_DB_PRIMARY_WRITE`，优先选择低风险写路径做灰度。
- DBA/运维 `009`：继续保留 `NORMALIZED_DB_WRITE=shadow` 观察 cron 健康监控；若出现实体表同步错误，可移除该 env 并重启后端回滚到只读灰度。
- 测试验收 `009`：补一次写后同步场景的验收记录，引用 `smoke-normalized-shadow-write.json` 和 36 表对账证据。

### 2026-06-22 `sessions/live` 实体表灰度读切换

目标：

- 在已完成 `reports/history`、`user/session-moment-summaries` 实体表灰度读后，继续把高频接口 `GET /api/v1/sessions/live` 纳入实体表读。
- 仍保持 `app_store` 为主写源，实体表通过 `NORMALIZED_DB_WRITE=shadow` 自动同步；本次不切实体表主写。

本轮修复：

- `backend/data/normalized-db.js`：`wine_session_members.meta_json` 同步补写成员 `meta` 与 `wheelHistory`，避免旧 `app_store` 成员历史转盘记录在实体表读侧丢失。
- `backend/data/normalized-read.js`：`getLiveSessionConfigFromNormalized()` 按当前线上 `front.js` 输出对齐 `/sessions/live`，兼容旧字符串 `meta_json` 和新对象 `meta_json`。
- `backend/server.js`：`/api/v1/sessions/live` 在 `NORMALIZED_DB_READ` 包含 `sessions` 时优先读实体表，失败自动回退旧 `app_store` 路径并记录 fallback。
- `backend/scripts/verify-normalized-session-read.js`：对默认会话、按 `sessionId`、按 `inviteCode` 三类调用做等价对账。

本地校验：

- `node --check backend/data/normalized-db.js`：通过。
- `node --check backend/data/normalized-read.js`：通过。
- `node --check backend/scripts/verify-normalized-session-read.js`：通过。
- `node --check backend/server.js`：通过。
- `npm.cmd run check:encoding`：通过。

线上同步与切换：

- 切换前对账证据：`/www/backup/jiuzhuopanguan/ops-009-normalized-session-read-20260622154500/verify-session-read.before-switch.json`。
  - `ok=true`
  - `caseCount=49`
  - `mismatchCount=0`
- 切换前 `.env` 备份：`/www/backup/jiuzhuopanguan/env.before-normalized-read-sessions-20260622155000`。
- 当前线上读开关：`NORMALIZED_DB_READ=reports,summaries,sessions`。
- 当前线上写同步开关：`NORMALIZED_DB_WRITE=shadow`。
- 仅重启 `jiuzhuopanguan-backend`；`pomer` 官网服务未重启。

切换后验证：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-session-read-20260622155000`。
- `verify-session-read.after-switch.json`：`ok=true`，`caseCount=49`，`mismatchCount=0`。
- `verify-history-read.after-session-switch.json`：`ok=true`，`checkedCount=24`，`profileCount=6`。
- `verify-summary-read.after-session-switch.json`：`ok=true`，`checkedCount=6`，`profileCount=6`。
- 健康监控：`/www/backup/jiuzhuopanguan/ops-009-health-20260622134242`。
- API 抽查：
  - `GET https://api.pomer.cn/api/v1/sessions/live?sessionId=session-1781756002741-4efdab`：HTTP 200，`status=已结束`，`inviteCode=7JMKE9`，`joinedCount=1`，`playerCount=2`。
  - `GET https://api.pomer.cn/api/v1/sessions/live`：HTTP 200，默认返回进行中样本 `session-1781689837491-97d578`，`inviteCode=U6G6NT`。

当前 PM 判断：

- 高频读对象拆表已覆盖三条用户高频读链路：`reports/history`、`user/session-moment-summaries`、`sessions/live`。
- 实体表仍不是业务主写源；当前状态是“实体表灰度读 + app_store 主写 + 实体表影子同步”。
- 本项不能标记为“彻底替代 app_store”，但已满足下一阶段主写/双写灰度的前置条件。

下一步责任：

- 后端/API `009`：继续推进实体表主写或双写灰度，优先选择创建聚会、上传 moment、生成 brief/share task 中风险最低的一条写路径。
- DBA/运维 `009`：继续观察 `NORMALIZED_DB_WRITE=shadow` 下的健康监控和 36 表对账；若发现同步错误，用 `.env` 备份回滚读写开关后仅重启 `jiuzhuopanguan-backend`。
- 测试验收 `009`：追加 `/sessions/live` 切换后冒烟记录，引用本节 API 抽查和 49 例等价对账证据。

### 2026-06-22 OSS DeleteObject 权限补齐与 smoke 对象清理

用户操作：

- 用户在阿里云 RAM 用户 `jzp-oss-uploader-prod` 下新增 `DeleteObject` 权限策略。

验证与清理：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-oss-delete-20260622162000`。
- 清理对象：`moments/session-1782103396743-3160eb/1782103398209-ops009-oss-smoke-1782103398099-67b747.webp`。
- `client.delete(objectKey)`：`ok=true`，HTTP status `204`。
- 删除后 `client.head(objectKey)`：`exists=false`，`code=NoSuchKey`，HTTP status `404`。

当前 PM 判断：

- OSS RAM 最小权限缺口已补齐：`PutObject` / `GetObject` / `DeleteObject` 已能覆盖当前 smoke 上传和清理链路。
- CDN 侧可能仍存在边缘缓存，这是 CDN 缓存行为；源站 OSS 对象已确认不存在。

下一步责任：

- DBA/运维 `009`：保留当前最小权限范围，不扩大到全 bucket `oss:*`；继续观察上传和清理链路。
- 接口联调 `009`：后续真实上传 smoke 可直接做上传后删除闭环，不再遗留测试对象。

### 2026-06-22 `user/share-image-summaries` 实体表灰度读切换

目标：

- 在 `reports/history`、`user/session-moment-summaries`、`sessions/live` 后，继续把分享图摘要高频读接口纳入实体表灰度读。
- 仍保持 `app_store` 为主写源，实体表通过 `NORMALIZED_DB_WRITE=shadow` 同步。

本轮新增/更新：

- `backend/data/normalized-db.js`：将 `share_image_tasks.created_at/started_at/finished_at/updated_at` 自动迁移为 `DATETIME(3)`，避免毫秒被截断导致等价对账漂移。
- `backend/data/normalized-read.js`：新增 `listUserShareImageSummariesFromNormalized()`，按房主或成员权限读取 `share_image_tasks`、`wine_sessions`、`wine_session_members`。
- `backend/server.js`：`GET /api/v1/user/share-image-summaries` 在 `NORMALIZED_DB_READ` 包含 `share-images` 时优先走实体表读，失败自动回退旧 `app_store` 路径。
- `backend/scripts/verify-normalized-share-image-summaries.js`：对所有 profile 做旧路径与实体表路径等价对账。
- `backend/package.json`：新增 `npm run db:verify-share-image-read`。

本地校验：

- `node --check backend/data/normalized-db.js`：通过。
- `node --check backend/data/normalized-read.js`：通过。
- `node --check backend/scripts/verify-normalized-share-image-summaries.js`：通过。
- `node --check backend/server.js`：通过。
- `npm.cmd run check:encoding`：通过。

线上切换：

- 切换前对账证据：`/www/backup/jiuzhuopanguan/ops-009-normalized-share-image-read-20260622163000/verify-share-image-read.before-switch.json`。
  - `ok=true`
  - `checkedCount=6`
  - `profileCount=6`
  - `mismatches=[]`
- 切换前 `.env` 备份：`/www/backup/jiuzhuopanguan/env.before-normalized-read-share-images-20260622163500`。
- 当前线上读开关：`NORMALIZED_DB_READ=reports,summaries,sessions,share-images`。
- 当前线上写同步开关：`NORMALIZED_DB_WRITE=shadow`。
- 仅重启 `jiuzhuopanguan-backend`；`pomer` 官网服务未重启。

切换后验证：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-share-image-read-20260622163500`。
- `verify-share-image-read.after-switch.json`：`ok=true`，`checkedCount=6`，`profileCount=6`，`mismatches=[]`。
- `verify-session-read.after-share-image-switch.json`：`ok=true`，`caseCount=49`，`mismatchCount=0`。
- `verify-history-read.after-share-image-switch.json`：`ok=true`，`checkedCount=24`，`profileCount=6`。
- `verify-summary-read.after-share-image-switch.json`：`ok=true`，`checkedCount=6`，`profileCount=6`。
- 健康监控：`/www/backup/jiuzhuopanguan/ops-009-health-20260622140641`。
- API 抽查证据：`api-share-image-summaries.json`。
  - token 后 8 位：`a9160666`。
  - `GET https://api.pomer.cn/api/v1/user/share-image-summaries`：HTTP 200。
  - profile：`user-1781294689996-6d192e`。
  - 返回 `items=1`，样本 `taskId=share-task-1781465622362-b11e8289`，`status=ready`。

当前 PM 判断：

- 高频读对象拆表已覆盖四条用户高频读链路：`reports/history`、`user/session-moment-summaries`、`sessions/live`、`user/share-image-summaries`。
- 实体表仍不是主写源；当前状态仍是“实体表灰度读 + app_store 主写 + 实体表影子同步”。

下一步责任：

- 后端/API `009`：继续推进实体表主写或双写灰度，优先从创建聚会或分享图任务写路径选择一条低风险路径。
- DBA/运维 `009`：继续观察 cron 健康监控和实体表对账；保留 `NORMALIZED_DB_WRITE=shadow` 作为主写切换前的稳定观察期。
- 测试验收 `009`：追加分享图摘要接口切换后的 API 抽查和 6 profile 等价对账证据。

### 2026-06-22 业务对象实体表影子写验证

目标：

- 补强 `NORMALIZED_DB_WRITE=shadow` 的业务链路证据，不只验证后台用户写入。
- 覆盖创建聚会、成员、moment、session event、brief、share task 等高频业务对象写入后，实体表能自动同步。

本轮新增/更新：

- `backend/data/content.js`：导出 `flushContentStore`，用于 smoke 等待 MySQL 持久化队列完成。
- `backend/data/social.js`：导出 `flushSocialStore`。
- `backend/data/moments.js`：导出 `flushMomentsStore`。
- `backend/scripts/smoke-moments-flow.js`：成功路径补 `process.exit(0)`，避免验证脚本因 MySQL 连接保持而挂住。
- `backend/scripts/smoke-normalized-business-shadow-write.js`：新增专用业务影子写 smoke。
- `backend/package.json`：新增 `npm run smoke:normalized-business-shadow`。

过程说明：

- 曾复用 `smoke-moments-flow.js` 做业务写验证，但发现旧脚本未显式等待 store flush，提前退出会导致 smoke session 残留在 `app_store`。
- 已清理残留 smoke session：`session-1782111269992-efc0b6`，同步实体表后 `verify-session-read` 恢复 `mismatchCount=0`。
- 后续改用专用脚本逐步 flush 并查实体表，避免异步持久化造成误判。

线上验证：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-normalized-business-shadow-20260622172000`。
- `smoke-normalized-business-shadow-write.json`：
  - `normalizedWrite=shadow`
  - 创建样本：
    - `sessionId=session-1782111642284-8ffdc3`
    - `momentId=moment-1782111643864-20ddfbac`
    - `eventId=event-1782111645327-ec5f410d`
    - `briefId=brief-1782111646769-ee80dbe1`
    - `shareTaskId=share-task-1782111648239-31b39b9e`
  - 插入验证：
    - `wine_sessions=1`
    - `wine_session_members=2`
    - `moment_records=1`
    - `session_events=1`
    - `session_briefs=1`
    - `share_image_tasks=1`
  - 清理后验证：上述 session/moment/event/brief/share task 对应实体表行均为 `0`。
  - `ok=true`
- `verify-normalized-shadow.after-business-shadow.json`：通过。
- `verify-session-read.after-business-shadow.json`：通过。
- `verify-share-image-read.after-business-shadow.json`：通过。
- 健康监控：`health-monitor.after-business-shadow.log`，输出 `/www/backup/jiuzhuopanguan/ops-009-health-20260622172025`。

当前 PM 判断：

- 实体表拆分已具备：
  - 36 张实体表同步对账；
  - 四条高频读接口灰度读实体表；
  - 高频业务对象写入后的影子写同步证据；
  - OSS/CDN 正式 provider 上传、读取和删除闭环；
  - Nginx/MySQL/PM2 备份、监控和恢复演练证据。
- 当前仍保留 `app_store` 为主写源，这是有意保守的灰度策略；本轮 009 目标可按“实体表拆分迁移与切换验证已完成当前阶段”处理，不应写成“app_store 已完全退役”。

下一步责任：

- 后端/API + DBA 下一阶段：如要继续退役 `app_store`，另开主写切换任务，设计 `NORMALIZED_DB_PRIMARY_WRITE` 或分对象主写策略。
- 测试验收 `009`：引用本节业务影子写证据，补充“业务写入后实体表同步和清理无残留”验收项。

### 2026-06-22 OSS 上传 E2E 最终复跑

背景：

- 旧证据 `/www/backup/jiuzhuopanguan/ops-009-oss-upload-20260622125500` 上传与 CDN HEAD 已通过，但当时 `DeleteObject` 权限缺失，报告整体不能作为最终闭环。
- 补齐 `DeleteObject` 后复跑发现 `smoke-oss-upload-http.js` 仍按旧合同只接受 HTTP 200；当前上传接口返回 HTTP 201，需更新 smoke 判定。
- `smoke-oss-upload-e2e.js` 成功路径未显式退出，且 cleanup 后未等待 store flush；已修复。

本轮修复：

- `backend/scripts/smoke-oss-upload-http.js`：上传成功 HTTP 状态接受 `200` 或 `201`。
- `backend/scripts/smoke-oss-upload-e2e.js`：cleanup 后等待 `flushAdminStore/flushMomentsStore/flushSocialStore`，成功时 `process.exit(0)`。

本地校验：

- `node --check backend/scripts/smoke-oss-upload-http.js`：通过。
- `node --check backend/scripts/smoke-oss-upload-e2e.js`：通过。
- `npm.cmd run check:encoding`：通过。

最终线上 E2E：

- 证据目录：`/www/backup/jiuzhuopanguan/ops-009-oss-upload-20260622180000`。
- `report.json`：
  - `ok=true`
  - `smokeExit=0`
  - `uploadHttp=201`
  - `provider=oss`
  - `url=https://cdn.pomer.cn/moments/session-1782112459649-56e4e0/1782112461643-ops009-oss-smoke-1782112461563-5a8acd.webp`
  - CDN HEAD：`status=200`，`Content-Type=image/webp`，`x-oss-cdn-auth=success`
  - `deletedObject=true`
  - PM2 前后重启均 `exit=0` 且 `apiReady=true`
- E2E 后残留对账：`/www/backup/jiuzhuopanguan/ops-009-post-final-oss-clean-20260622180500`。
  - `verify-normalized-shadow.json`：通过。
  - `verify-session-read.json`：通过，`mismatchCount=0`。
  - `verify-share-image-read.json`：通过。
  - 健康监控通过。

当前 PM 判断：

- OSS/CDN 生产链路已闭环：RAM env、上传到 OSS、返回 CDN URL、CDN 可读、OSS 删除清理、PM2 重启后 API 可用均有最终证据。
- 旧 `DeleteObject` 缺口已关闭。

## 2026-06-22 上线前清库

用户指令：

- 用户确认当前线上数据均为测试数据，要求直接清空数据库用于上线准备。

执行前备份：

- 完整备份目录：`/www/backup/jiuzhuopanguan/prelaunch-clear-20260622183000/ops-009-20260622153515`。
- 包含：
  - MySQL 全库 dump：`mysql/jiuzhuopanguan.20260622153515.sql.gz`
  - `app_store` 单独 dump：`mysql/app_store.20260622153515.sql.gz`
  - Nginx 配置备份
  - PM2 状态与日志
  - 健康检查证据

执行动作：

- 目标库确认：`MYSQL_DATABASE=jiuzhuopanguan`。
- 只操作 `/www/wwwroot/jiuzhuopanguan-git/backend` 和 `jiuzhuopanguan-backend`。
- 未改动、未重启 `pomer` 官网服务。
- 新增并执行：`backend/scripts/ops-prelaunch-clear-database.js`。
- 执行证据目录：`/www/backup/jiuzhuopanguan/prelaunch-clear-exec-20260622184000`。
- 清空前备份了 5 个 store 镜像：
  - `admin-store.json`
  - `asset-manifest.json`
  - `content-store.json`
  - `moments-store.json`
  - `social-store.json`
- 清空动作：
  - TRUNCATE 当前库全部表。
  - 删除线上 5 个旧 store 镜像，避免服务重启后从测试 JSON 镜像回灌旧数据。
  - 重启 `jiuzhuopanguan-backend --update-env`。

执行结果：

- `clear-report.json`：所有表清空后计数均为 `0`，`ok=true`。
- 后端重启成功：`jiuzhuopanguan-backend` online。
- 重启后服务按代码默认值重建最小基线：
  - `app_store=5`
  - `admin_users=2`
  - `admin_roles=3`
  - `points_tasks=1`
  - `ranking_reward_rules=7`
- 业务测试数据表均为 `0`：
  - `users=0`
  - `user_sessions=0`
  - `user_login_logs=0`
  - `friendships=0`
  - `poke_threads=0`
  - `wine_sessions=0`
  - `wine_session_members=0`
  - `wine_reports=0`
  - `moment_records=0`
  - `session_events=0`
  - `session_briefs=0`
  - `share_image_tasks=0`
  - `moment_nominations=0`
  - `ranking_reward_payouts=0`
  - `points_ledger=0`
  - `analytics_events=0`

上线前验证：

- `GET https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- `GET https://cdn.pomer.cn/cdn-check.png`：HTTP 200。
- PM2：`jiuzhuopanguan-backend` online；`pomer` 官网服务 online 且未重启。

当前 PM 判断：

- 线上测试业务数据已清空。
- 系统保留了代码默认的最小后台与规则基线，便于上线后继续登录和配置。
- 旧测试 store 镜像已备份并移除，避免重启回灌测试数据。

## 2026-06-22 创建聚会 SESSION NOT FOUND 热修

用户反馈：

- 创建聚会页面弹出 `SESSION NOT FOUND`。

原因判断：

- 线上已开启 `NORMALIZED_DB_READ=sessions`，创建接口写入主存储后会异步影子同步实体表。
- 小程序创建成功后原逻辑会立即二次请求 `/api/v1/sessions/live?sessionId=...`。
- 在实体表同步短暂延迟窗口内，`/sessions/live` 命中实体表读取但未命中该新 session，旧逻辑直接返回 404 `session not found`，导致创建页弹错。

处理动作：

- 后端：`backend/server.js` 的 `/api/v1/sessions/live` 在实体表未命中指定 `sessionId/inviteCode` 时，改为回退查询主存储；只有实体表和主存储均未命中才返回 404。
- 前端：`miniprogram/services/operations.ts` 的 `createManagedSession()` 改为直接使用 `POST /sessions` 返回的 live session 数据，避免创建后立刻二次 GET 撞同步窗口。
- 线上部署：已备份线上 `server.js` 至 `/www/backup/jiuzhuopanguan/session-not-found-20260622155032/server.js.before`，上传热修并只重启 `jiuzhuopanguan-backend`。
- 未改动、未重启 `pomer` 官网服务。

验证：

- 本地 `node --check backend/server.js`：通过。
- 本地 `npm.cmd run typecheck`：通过。
- 本地 `npm.cmd run check:encoding`：通过。
- 服务器 `node --check server.js`：通过。
- PM2：`jiuzhuopanguan-backend` online。
- 微信开发者工具自动化端口 `9420` 已恢复，可读取当前登录态；证据只记录 token 后 8 位 `c19e951d`。
- 线上真实 smoke：
  - `POST https://api.pomer.cn/api/v1/sessions`：`code=0`，创建 `session-1782114945558-4dc956`。
  - 立即 `GET https://api.pomer.cn/api/v1/sessions/live?sessionId=session-1782114945558-4dc956`：`code=0`，返回同一 `sessionId`，未再出现 404。
  - 已调用 `DELETE /api/v1/sessions/session-1782114945558-4dc956` 清理 smoke 数据，`removed=true`。

当前 PM 判断：

- 线上后端 `SESSION NOT FOUND` 竞态已热修并验证通过。
- 小程序前端侧也已本地修复；需要微信开发者工具重新编译后再由用户侧点击创建聚会确认 UI 不再弹错。

## 2026-06-22 时间线照片不显示修复

用户反馈：

- 聚会记录页“开场打卡”卡片有相框但不显示上传图片。

原因判断：

- 当前页面数据中 `recordTimelineDisplayItems[0].imageUrl` 已存在，且 `imageBroken=false`。
- CDN 图片 `https://cdn.pomer.cn/moments/session-1782115376791-57078d/1782115396176-moment-1782115396869-e3b915.webp` 返回 HTTP 200，文件内容正常。
- 真实原因是 `live-record` 页面照片图层 `z-index=1`，相框 SVG 图层 `z-index=2`，相框中间含浅色占位底，盖住了真实照片。

处理动作：

- 更新 `miniprogram/pages/live-record/index.less`：将 `.live-record008as-page .live-record-photo-image, .live-record008as-photo-failed` 的层级提升为 `z-index: 3`，仍低于胶带/贴纸的 `z-index: 4`。

验证：

- `npm.cmd run check:encoding`：通过。
- `npm.cmd run typecheck`：通过。
- 微信开发者工具自动化读取页面数据：`imageUrl` 有值，`imageBroken=false`。
- 截图证据：`docs/runtime/wechat-session-image-fixed.png`，照片已显示在相框内。

当前 PM 判断：

- 小程序记录页照片不显示问题已修复并在开发者工具预览框验证通过。

## 2026-06-22 分享图权限与首页封面 PM 总台账更新

触发背景：

- 用户确认小程序已发布上线，并追问“房主/成员/聚会/分享”链路是否存在隐藏漏洞。
- 用户进一步明确产品规则：成员应必须等房主结束聚会后，才可以查看、保存或分享最终分享图。
- 用户反馈首页“待分享回忆”封面仍未使用第一张上传图。

已核验证据：

- 最新线上部署版本：`c9dc0b05 fix(home): use first uploaded photo as memory cover`。
- 近期相关提交：
  - `f51ec12b fix(invite): support host member removal`
  - `641d760b fix(invite): sync removed members to normalized storage`
  - `0858f8be fix(invite): harden invite membership visibility`
  - `c9dc0b05 fix(home): use first uploaded photo as memory cover`
- 线上部署验证：
  - `jiuzhuopanguan-backend` 已部署到 `api.pomer.cn` 对应服务并在线。
  - `GET https://api.pomer.cn/api/v1/config/home` 返回 HTTP 200。
  - 本次部署未改动、未重启、未代理 `pomer.cn` 公司官网服务。
- 首页封面链路：
  - 前端首页 `mapRecentAlbumsFromSummaries()` 已优先读取 `coverPhotoUrl || readyShareImageUrl || shareImageUrl`。
  - 后端 `backend/data/moments.js` 与 `backend/data/normalized-read.js` 已补齐 `coverPhotoUrl`，取同一 `sessionId` 下最早一张有 `imageUrl` 的上传照片。
  - 该修复为后端字段补齐，小程序包无需因本次封面修复单独重发；用户重新进入首页或刷新后应读取新字段。
- 邀请/踢人链路：
  - 房主可在邀请页踢出成员。
  - 踢出后删除当前 `sessionId + profileId` 成员绑定，并同步删除 normalized/MySQL `wine_session_members` 对应记录。
  - 被踢用户不能从自己的“我的/历史/分享图合集/直接 sessionId 入口”继续进入该局。
  - 被踢用户仍可从分享链接/口令重新加入；重新加入成功后恢复该局成员关系。
  - 无 token 或非成员通过 inviteCode 查询时，后端只返回邀请必要信息，`joinedPlayers/joinStatusPlayers` 为空，避免暴露成员头像/昵称。

当前未闭环权限缺口：

- 当前分享图后端权限不是“仅房主可生成/保存/分享”。
- `createShareImageTask()` 当前调用 `getSessionBrief()`，而 `getSessionBrief()` / `createOrRefreshSessionBrief()` 使用 `assertSessionMember()`，即本局成员即可生成分享图任务。
- `getShareImageTask()`、`retryShareImageTask()`、`processShareImageTask()` 等任务读取/刷新/处理路径同样按成员校验，不是房主校验。
- 前端分享图页 `handleSaveTap()` 本地保存动作只依赖任务 ready 与相册权限；只要成员能进入分享图页并拿到 ready task，就可能保存。
- `getUserShareImageSummaries()` / normalized 版本当前允许房主或成员看到该局 ready 分享图。
- 进行中状态页的分享 tab/按钮虽已按用户要求移除，但后端尚未强制“聚会未结束不得创建/刷新/保存最终分享图”。如果旧入口、历史链接或直接参数打开分享页，仍存在绕过前端入口的风险。

PM 判断：

- 首页第一张上传图作为封面：后端字段已修复并上线，待前端/测试在已发布小程序或开发者工具中刷新验证。
- 成员分享权限：不能标记完成。当前只能说“主 UI 链路不鼓励进行中分享”，不能说“后端已禁止成员或进行中分享”。
- 新增权限 P0：分享图必须改为结束后开放；未结束时所有角色不得创建最终分享图任务。是否仅房主可生成，仍需按用户产品口径最终确认；当前用户明确的是“成员必须等房主结束聚会后才可以分享”。

下一步责任人：

- 后端/API 负责人 `SHARE-AUTH-011`：
  - 在后端强制校验 session 状态：未结束时 `createShareImageTask()`、`retryShareImageTask()`、`processShareImageTask()` 返回明确错误，如 `409 session not ended`。
  - 保持被踢成员不可访问分享图合集和任务详情。
  - 若产品决定“只有房主可生成最终分享图”，生成/重试/处理接口需改为 `assertSessionHost()`；若允许结束后成员保存，则读取 ready task 可继续按成员校验，但创建权限仍应收紧。
  - 补本地 smoke：进行中成员/房主创建分享图应失败；结束后房主创建成功；结束后成员按最终规则可读或不可读；被踢用户不可读。
- 前端负责人 `SHARE-AUTH-011-FE`：
  - 等后端合同确认后，隐藏进行中分享图入口和生成/保存按钮。
  - 分享页遇到 `session not ended` 或权限错误时展示“聚会结束后可查看分享图”，不能让按钮停留在可点击状态。
  - 已发布小程序若涉及前端改动，需要重新上传小程序包。
- 接口联调负责人 `SHARE-AUTH-011-INT`：
  - 后端补齐后，用有效 `jzp-user-token` 只读/写入最小样本验证：进行中禁止、结束后允许、被踢禁止、重新通过 inviteCode 加入后恢复归属。
  - 证据只记录 token 后 8 位，不泄露完整 token。
- 测试验收负责人 `SHARE-AUTH-011-QA`：
  - 开发者工具预览框复测：进行中无分享入口；结束聚会后跳转分享页；成员在结束前不能生成/保存；被踢后不能从“我的/历史/分享图合集”进入。
  - 复测首页“待分享回忆/最近相册”封面是否使用首张上传图。
- UI/UX 负责人 `SHARE-AUTH-011-UX`：
  - 如后端返回未结束/无权限，补齐分享页提示文案和按钮状态建议，不改业务源码。

## 2026-06-23 SHARE-AUTH-011 后端合同与实现推进

本轮核查：

- 后端合同与实现已在本地 `HEAD`：`94960a43 backend: enforce ended share image auth`。
- 涉及文件：
  - `backend/data/moments.js`
  - `backend/scripts/smoke-share-image-async-flow.js`
  - `docs/runtime/pr-backend-share-auth-011.md`
- 已推送远端：`origin/main` 已从 `7e60c4d9` 更新到 `94960a43`。

后端合同：

- 未结束 session：
  - `createShareImageTask()` 返回 `409 session not ended`。
  - `retryShareImageTask()` 返回 `409 session not ended`。
  - `processShareImageTask()` 返回 `409 session not ended`。
- 已结束 session：
  - 房主可创建、重试、处理分享图任务。
  - 普通成员创建分享图任务返回 `403 forbidden`。
  - 成员仍可读取 ready 分享图任务详情与分享图合集。
- 被踢成员：
  - 读取分享图任务返回 `403 not session member`。
  - `GET /api/v1/user/share-image-summaries` 不再出现该局分享图。

本地验证：

- `node --check backend/data/moments.js`：通过。
- `node --check backend/scripts/smoke-share-image-async-flow.js`：通过。
- `npm.cmd run check:encoding`：通过。
- `node backend/scripts/smoke-share-image-async-flow.js`：通过，`ok=true`。

当前阻塞：

- 当前 PM 线程本机连接 `root@api.pomer.cn` 失败：`Permission denied (publickey)`。
- 因此本轮只完成本地验证与远端推送，尚未由当前线程完成线上 `api.pomer.cn` 部署重启。

已通知责任人：

- DBA/运维 `SHARE-AUTH-011-OPS`：拉取 `origin/main`、只重启 `jiuzhuopanguan-backend`、补 PM2/API 健康证据；不得触碰 `pomer.cn` 官网服务。
- 接口联调 `SHARE-AUTH-011-INT`：等待部署证据后按矩阵复跑，证据只写 token 后 8 位。
- 前端 `SHARE-AUTH-011-FE`：按 `409 session not ended`、`403 forbidden`、`403 not session member` 接入分享页错误态和按钮禁用。
- 测试验收 `SHARE-AUTH-011-QA`：等待 DBA 部署、接口矩阵、前端状态证据后再复测，不提前写通过。

## 2026-06-23 负责人未入总台账改动补录

本节用于补齐各负责人已交付但尚未汇总到 PM 总台账的证据。仅做 PM 汇总，不覆盖各角色自己的交付记录。

### 已补录角色证据

- 后端/API `SHARE-AUTH-011`：
  - 证据文件：`docs/runtime/pr-backend-share-auth-011.md`。
  - 代码合同：分享图生成、重试、处理必须满足“用户仍是成员 + session 已结束 + 用户是房主”；未结束返回 `409 session not ended`；结束后普通成员创建/重试/处理返回 `403 forbidden`；成员仍可读取 ready 分享图，被踢后不再可读。
  - 本地验证：`node --check backend/data/moments.js`、`node --check backend/scripts/smoke-share-image-async-flow.js`、`npm.cmd run check:encoding`、`node backend/scripts/smoke-share-image-async-flow.js` 通过。
  - 状态：本地合同与 smoke 已完成；线上是否已部署需以 DBA/运维或 PM2/API 证据为准。
- 前端 `SHARE-AUTH-011-FE`：
  - 证据文件：`docs/runtime/pr-fe-share-auth-011.md`。
  - 接入点：`miniprogram/pages/share-poster/index.ts|wxml|less` 已处理 `session not ended`、`forbidden`、`not session member` 错误态，阻断生成/保存/刷新；首页、我的、相册读取 `coverPhotoUrl` 作为首图封面。
  - 验证：`npm.cmd run typecheck`、`npm.cmd run check:encoding` 通过；首页窗口截图文件已生成。
  - 阻塞：9420 返回 ticket 但 WebSocket candidate 不可用，未完成完整自动化截图；仍需线上后端部署后复测真实 `409/403` 合同；涉及前端包，后续如确认上线需重新上传小程序包。
- 接口联调 `SHARE-AUTH-011-INT`：
  - 证据文件：`docs/runtime/pr-int-share-auth-011.md`。
  - 已建立部署后矩阵：进行中禁止创建/retry/process、结束后房主允许、成员创建禁止但 ready 可读、被踢不可读、被踢 summaries 移除、重新通过 inviteCode 加入后恢复归属、normalized 与 app_store 对账。
  - 状态：准备完成；等待线上部署证据、有效 token、最小样本与清理方案后复跑，不得用本地 smoke 冒充线上验收。
- UI/UX `SHARE-AUTH-011-UX`：
  - 证据文件：`docs/runtime/pr-uiux-share-auth-011.md`。
  - 产出：未结束、无权限、被踢出三类分享页状态文案和按钮状态建议；不改业务源码，不改 PM 总台账。
  - 状态：建议已交付，等待前端按合同落地和 QA 复测。
- 测试验收 `SHARE-AUTH-011-QA`：
  - 证据文件：`docs/runtime/pr-qa-share-auth-011.md`。
  - 当前状态：`waiting / blocked`。
  - 阻塞原因：缺线上部署证据、缺接口联调线上矩阵、缺前端真实预览框错误态证据；不能写通过、上线准出或正式真机发布通过。
- 前端返回挂起复测 `011`：
  - 证据文件：`docs/runtime/pr-fe-return-suspend-retest-011.md`，窗口截图 `docs/runtime/pr-fe-return-suspend-current-window.png`。
  - 覆盖 commit：`dbbc4d02 fix(frontend): preserve active session on back navigation`。
  - 静态核查：创建页已有未结束 runtime 时回记录页；邀请页/等待页/记录页顶部返回保留 runtime；邀请页、等待页、记录页禁用侧滑；首页挂起条回记录页。
  - 验证：`npm.cmd run typecheck`、`npm.cmd run check:encoding`、目标文件 `git diff --check` 通过。
  - 阻塞：9420 端口可连，但 `/v2/auto` 未暴露可用 WebSocket candidate，未完成真实点击链路和侧滑交互验证。

### 当前新改动：首拍后才计入进行中

用户最新规则：

- 拍照上传并保存成功第一张照片后，聚会才算“进行中”。
- 创建页面、邀请页面、拍照页面在首拍前返回，不应计入进行中。
- 首拍前返回必须给玩家明确提示。

PM 当前线程已完成本地前端改动，尚未提交、尚未部署、尚未上传小程序包：

- 新增本地首拍标记：`miniprogram/utils/session.ts` 增加 `firstPhotoUploadedAt`、`hasSessionFirstPhoto()`、`isSessionRuntimeInProgress()`、`markSessionFirstPhotoUploaded()`。
- 首页挂起条：`miniprogram/utils/session-return.ts` 改为只有 `firstPhotoUploadedAt` 存在且未结束才展示。
- 创建页：`miniprogram/pages/create-session/index.ts|wxml` 首次创建只写 `startedAt=0` 与 `待首拍`，顶部返回和系统返回提示“首张照片保存前不计入进行中”。
- 邀请页/记录页：`miniprogram/pages/invite-group/index.ts`、`miniprogram/pages/live-record/index.ts` 首拍前返回文案改为“不会计入进行中”，首拍后仍为挂起离开。
- 拍照页：`miniprogram/pages/moment-editor/index.ts` 首拍前必须上传图片；保存成功并有 `imageUrl` 后写入首拍标记和开始时间；首拍前返回提示不会进入继续记录入口。
- 首页/我的/相册：`miniprogram/pages/index/index.ts`、`miniprogram/pages/me/index.ts`、`miniprogram/pages/album/index.ts` 的进行中统计、待分享回忆、相册 ongoing 筛选均排除无首图空聚会。

本地验证：

- `npm.cmd run check:encoding`：通过。
- `npm.cmd run typecheck`：通过。
- `git diff --check -- <本轮 11 个改动文件>`：通过。
- `npm.cmd run build`：未执行成功，原因是仓库没有 `build` script；`npm.cmd run` 显示仅有 `check:encoding`、`wechat:auto`、`typecheck`。

当前工作区状态：

- `HEAD/main/origin/main` 当前为 `7c9c7e2 fix(frontend): block unauthorized share poster actions`。
- 本轮首拍规则改动仍是未提交 tracked diff，涉及 11 个小程序前端/工具文件。
- 当前还存在 3 个未提交 tracked diff，PM 本轮未写入，需由新 PM/对应负责人复核来源后再决定是否提交：
  - `backend/data/admin.js`
  - `backend/data/moments.js`
  - `backend/public/admin/static/heatwave-ops/app.js`
- 仍存在若干未跟踪历史证据/资产文件，PM 未将其纳入本轮提交范围，避免混入旧证据：
  - `docs/design-assets/party-recorder/story-comic-20260622/`
  - `docs/runtime/pr-fe-return-suspend-current-window.png`
  - `docs/runtime/pr-fe-return-suspend-retest-011.md`
  - `docs/runtime/pr-fe-share-auth-011-home-cover-window.png`
  - `docs/runtime/pr-fe-share-auth-011-home-cover.png`
  - `docs/runtime/pr-fe-share-auth-011.md`
  - `docs/runtime/pr-int-share-auth-011.md`
  - `docs/runtime/pr-qa-share-auth-011.md`
  - `docs/runtime/pr-uiux-share-auth-011.md`
  - `miniprogram/assets/party-recorder/party-recorder-app-icon-144.png`

### PM 当前判断

- `SHARE-AUTH-011`：各负责人证据已补入 PM 总台账，但仍不能整体写通过；上线闭环取决于线上部署、接口联调矩阵和 QA 预览框复测。
- `返回挂起 011`：已有静态核查和命令验证，真实点击与侧滑仍待工具恢复后 QA 补测。
- `首拍后才计入进行中`：本地代码和基础检查已完成，但未提交、未推送、未上传小程序包，也未做开发者工具点击复测；应交给前端/QA 继续验收。
- 新 PM 总控线程已创建并置顶：`019ef398-1fe1-7423-9579-8afe0b54f353`，标题 `PM总控-聚会记录师-接管20260623`。

### 接下来谁做什么

- PM 新总控线程 `019ef398-1fe1-7423-9579-8afe0b54f353`：接管本文件，继续以本节为当前事实基线；不得通读旧长文档，优先读 `AGENTS.md`、`PRD.md`、`docs/runtime/pm-active-worklog.md` 和本轮短证据文件。
- 前端负责人：复测并补证 `首拍后才计入进行中`，重点覆盖创建页返回、邀请页返回、拍照页未上传返回、首张照片保存后首页挂起条出现、我的/相册进行中计数变化；如通过，提交前端证据文件。
- 测试验收负责人：在前端证据到位后，用微信开发者工具预览框执行点击链路；未完成前保持 waiting，不写通过。
- 接口联调负责人：继续等待线上 `SHARE-AUTH-011` 部署证据后复跑矩阵；所有 token 只写后 8 位。
- DBA/运维负责人：如用户要求线上部署，先确认仅部署 `api.pomer.cn` / `jiuzhuopanguan-backend`，不得触碰 `pomer.cn` 官网。

## 2026-06-23 首拍进行中与审核分享候选线上部署

执行边界：

- 仅面向 `api.pomer.cn` 对应的 `jiuzhuopanguan-backend` 服务。
- 使用历史记录确认的 SSH 别名 `pomer.cn` 登录同一服务器，但只操作 `/www/wwwroot/jiuzhuopanguan-git` 与 PM2 `jiuzhuopanguan-backend`。
- 未改动、未重启、未代理 `pomer.cn` 公司官网目录、Nginx 或 PM2 `pomer` 服务。

GIT：

- 本地已推送：`5bcfebda feat(frontend): gate active sessions on first photo`。
- 服务器执行 `git pull --ff-only origin main`，从 `7c9c7e2` fast-forward 到 `5bcfebd`。

备份：

- 线上备份目录：`/www/wwwroot/jiuzhuopanguan-git/backend/backups/codex-deploy-20260623204311`。
- 已保存部署前 `git status`、`git head`、PM2 `jlist`。

部署命令摘要：

- `node --check backend/data/admin.js`
- `node --check backend/data/moments.js`
- `node --check backend/public/admin/static/heatwave-ops/app.js`
- `cd backend && npm install`
- `npm run mysql:test`
- `pm2 restart jiuzhuopanguan-backend --update-env`

验证结果：

- `npm install`：`found 0 vulnerabilities`。
- `npm run mysql:test`：`ok=true`，表 `app_store`，`total=5`。
- PM2 `jiuzhuopanguan-backend`：online，script path `/www/wwwroot/jiuzhuopanguan-git/backend/server.js`，unstable restarts `0`。
- PM2 `pomer` 官网服务：online，uptime 10D，restarts `0`，未重启。
- 公网 `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- 公网 `https://api.pomer.cn/api/v1/config/templates`：HTTP 200。
- 公网 `https://api.pomer.cn/admin/login`：HTTP 200。
- 公网 `https://api.pomer.cn/admin/static/heatwave-ops/app.js`：HTTP 200。

服务器工作区残留说明：

- 部署后服务器 `git status` 仍显示运行期数据文件与上传文件：`backend/data/social-store.json`、`backend/package-lock.json`、`backend/backups/`、`backend/public/uploads/**`。
- 本次未清理这些运行期数据，避免误删线上用户上传、备份或 store 镜像。

小程序包状态：

- 本机未找到可执行的微信开发者工具 `cli.bat` 或小程序 CI 上传脚本/上传密钥。
- 因此本次已完成 Git 推送与 `api.pomer.cn` 后端/后台静态资源部署；小程序前端包仍需通过微信开发者工具人工上传，或补齐 CI 上传密钥后再由 PM/前端执行上传。

## 2026-06-23 全量交互接口后台联通审核派发

触发原因：

- 用户要求 UI 全量审核所有页面用户交互、界面设计、返回和进入新页面逻辑。
- 用户要求接口联调审核前后端接口是否全部对齐。
- 用户要求后台管理审核后台系统参数是否都能在小程序前端联通。
- 用户要求各职责分别建立记录文档，PM 再逐项分析并分派负责人修补。

派发状态：

- UI/UX 负责人线程 `019eebc6-b453-74a2-b717-b3c54897d395`：已派发 `FULL-UIUX-AUDIT-013`。
  - 指定记录文档：`docs/runtime/uiux-full-interaction-audit-20260623-2102.md`。
  - 审核范围：小程序所有页面用户交互、界面设计、返回逻辑、进入新页面逻辑；重点覆盖首拍前不计入进行中、旧参与房间不得拦截新建聚会、精彩瞬间/推举/分享授权/双审通过链路。
  - 证据要求：问题编号、页面/路径、现象、用户影响、严重级别、证据来源、预期行为、疑似责任角色、建议修补方向；未覆盖预览框点击时标记“静态审核/待预览框复核”。
- 接口联调负责人线程 `019eebc7-16dd-7cf0-bf03-1114f6a4881e`：已派发 `FULL-INT-AUDIT-013`。
  - 指定记录文档：`docs/runtime/int-api-contract-audit-20260623-2102.md`。
  - 审核范围：小程序 API 调用与后端路由、方法、鉴权、请求字段、响应字段、错误态、线上只读证据是否对齐。
  - 证据要求：前端调用位置、后端处理位置、请求/响应/错误态、严重级别、影响链路、疑似责任角色、建议修补方向；敏感值只写后 8 位或摘要。
- 后台管理负责人线程 `019eebc7-3b42-7290-8abc-6bc533117144`：已派发 `FULL-ADMIN-LINKAGE-AUDIT-013`。
  - 指定记录文档：`docs/runtime/admin-frontend-parameter-linkage-audit-20260623-2102.md`。
  - 审核范围：后台配置、参数、审核动作、内容管理、精彩瞬间审核、分享授权/公开候选、榜单/奖励资格、首页配置、模板配置、运营参数是否联通小程序前端。
  - 证据要求：后台页面/参数/动作、后端数据字段/API、前端消费页面/字段、联通状态、证据来源、严重级别、影响、疑似责任角色、建议修补方向。

PM 边界：

- 本轮 PM 只派发和记录，不代替角色审核、不改业务源码、不直接写跨角色完成结论。
- 三份记录产出后，PM 再逐项读取并按 P0/P1/P2/P3、证据完整度和责任边界分派前端、后端/API、接口联调、后台管理、测试验收或 UI/UX 修补。
- 对缺少预览框点击、线上接口矩阵、后台写入清理证据的问题，只能标记待复核/待联调，不写通过或上线准出。

## 2026-06-23 FIX-014-01 用户审批与后端派发

- 用户已明确同意 `FIX-014-01`；其余修复项和验收项仍为待审批。
- 目标问题：`GET /api/v1/sessions/live` 匿名无 token、无 `sessionId/inviteCode` 时可返回任意进行中聚会摘要，来源为 `FULL-INT-AUDIT-013-P0-01`。
- 已派发后端/API 负责人线程 `019eebc6-f69d-73b2-a5d3-7f8e581283ac`，要求只处理无参匿名、`sessionId` 成员校验、`inviteCode` 匿名裁剪和聚焦 smoke。
- 后端证据目标：`docs/runtime/pr-backend-fix-014-01-20260623.md`。
- 当前状态：后端实施中；未提交、未推送、未部署、未做线上回归，不能写修复完成。
- 后续门禁：PM 复核本地代码和测试 -> 接口联调检查 -> 独立提交/推送 -> 仅部署 `api.pomer.cn` 的 `jiuzhuopanguan-backend` -> 线上只读回归 -> QA 验收。

### FIX-014-01 本地实现与复核进展

- 后端交付：`backend/server.js`、`backend/data/live-session-access.js`、`backend/scripts/smoke-live-session-privacy.js`，证据 `docs/runtime/pr-backend-fix-014-01-20260623.md`。
- 合同结果：无参 400；匿名 `sessionId` 401；非成员 403；成员 200；`sessionId+inviteCode` 仍按私有读取强制鉴权；仅 `inviteCode` 匿名只返回公开白名单。
- PM 第一次退回：app_store 缺记录而 normalized 命中时可能 fail-open；后端已改为无法确认成员时公开裁剪。
- 接口联调第一次退回：app_store 与 normalized 返回对象身份不一致时仍可能返回私有视图；后端已新增实际返回对象的 session ID、inviteCode、成员身份一致性策略和可执行 mismatch 用例。
- Smoke 过程问题：首次并发双跑共享 JSON store 产生 stamp `1782222609946` 的 3 个测试 profile；该证据已作废，负责人已按唯一 stamp 定向清理，并改为动态端口、排他锁、串行执行。
- PM 独立最终复跑：`ok=true`；400/401/403/200 矩阵通过；normalized/app_store mismatch 返回 public；编码检查通过；`FINAL_DATA_MARKERS_CLEAR`、`FINAL_LOCK_CLEAR`，动态端口无监听。
- 当前阻塞：接口联调线程 `019eebc7-16dd-7cf0-bf03-1114f6a4881e` 在后端补交后的两次复核指令均返回 `systemError`。按规则不得由 PM 冒充接口角色签字，当前保持“后端本地通过、接口联调待恢复、线上未部署”。
- Git 门禁：本地 `main` 另有非本任务、未推送提交 `ae98afd5 fix(frontend): recover expired user sessions`。未确认归属前不得将本任务连同该提交一起推送。

### FIX-014-01 阻塞收口

- 接口联调负责人首轮已完成 app_store 本地矩阵并退回 normalized/app_store 身份不一致问题，证据为 `docs/runtime/pr-int-fix-014-01-20260623.md`。
- 后端已按退回要求新增实际返回对象一致性策略与可执行测试，PM 独立复跑通过。
- PM 随后向接口联调线程发出三次补交复核指令，线程均直接返回 `systemError`，未产生最终复核结论。
- 按“同一任务连续阻塞/收口”规则，当前停止继续重试并标记阻塞；不得由 PM 代替接口联调签字，不得写线上通过。
- 当前交付状态：本地代码和后端证据已完成；接口联调最终证据缺失；未提交、未推送、未部署；线上 P0 仍未消除。
- 恢复条件：接口联调线程恢复，或用户明确授权更换接口联调执行载体；恢复后先复核本地补交，再处理 Git 中非本任务提交 `ae98afd5` 的推送边界，最后才能部署 `api.pomer.cn` 并做线上只读回归。

### FIX-014-01 恢复后接口联调复核

- 2026-06-24：用户确认“线程已恢复请继续”；PM 先向原接口联调线程 `019eebc7-16dd-7cf0-bf03-1114f6a4881e` 补发复核指令，但线程状态仍为 `systemError`，未产出新回复或新证据。
- 2026-06-24：按用户此前“已链接/继续”和阻塞恢复条件，PM 启动替代接口联调执行载体，仅授权写 `docs/runtime/pr-int-fix-014-01-20260623.md`，不改业务源码、不提交、不推送、不部署。
- 替代接口联调结论：`FIX-014-01` 本地合同通过，线上待部署回归。证据文档已更新为“本地合同通过，线上待部署回归”，并保留首轮退回历史。
- 替代接口联调验证：`node --check backend/data/live-session-access.js`、`node --check backend/server.js`、`node --check backend/scripts/smoke-live-session-privacy.js`、`node backend/scripts/smoke-live-session-privacy.js` 均通过；smoke 动态端口 `5251`，`ok=true`。
- 合同矩阵：无参 400；匿名 `sessionId` 401；非成员 `sessionId` 403；成员 `sessionId` 200；匿名 `sessionId+inviteCode` 401；匿名 `inviteCode` 仅 13 个公开白名单字段；`normalizedAppStoreIdentityMismatchDefaultsToPublic=true`。
- PM 同步复核：本地再次串行 smoke 通过，动态端口 `4341`，编码检查与 typecheck 通过；`backend/data/**/*.json` 未检出 `privacy-smoke/live-privacy-` marker，lock 不存在。
- 当前状态：本地合同通过，待提交、推送、部署 `api.pomer.cn` 的 `jiuzhuopanguan-backend`，随后做线上只读回归和 QA 验收。不得写线上通过或正式准出。
- Git 门禁仍需处理：本地 `main` 领先远端的非本任务提交 `ae98afd5 fix(frontend): recover expired user sessions` 不得未经确认随本任务推送；如需立即部署 FIX-014-01，应从 `origin/main` 生成只含本修复的提交并推送部署。

### FIX-014-01 提交部署与线上回归

- 2026-06-24：PM 为避开本地 `main` 上未确认的非本任务提交 `ae98afd5`，从 `origin/main` 建立干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-01`，提交 `759be62 fix(backend): protect live session reads` 并推送到远端 `main`。
- 推送边界：`git log origin/main..HEAD` 在干净工作树仅包含 `759be62`；未把 `ae98afd5 fix(frontend): recover expired user sessions` 随本次推送。
- 部署目标：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`；未触碰 `pomer.cn` 官网 Nginx、官网目录或官网 PM2 服务。
- 服务器备份：`/www/backup/jiuzhuopanguan/fix-014-01-20260624-004948`，记录 `head-before=33343a25aabb216c61df84a495f836e37b310bfa`、`origin-main-before=759be62ee34e9af2825363f59326f21693812ff2`、PM2 jlist、`server.js.before`、`social-store.json.before` 与运行期 store diff。
- 部署命令摘要：`git pull --ff-only origin main`、`cd backend && npm install`、`node --check server.js`、`node --check data/live-session-access.js`、`node --check scripts/smoke-live-session-privacy.js`、`. ./.env && npm run mysql:test`、`pm2 restart jiuzhuopanguan-backend --update-env`。
- 部署验证：服务器 HEAD 为 `759be62ee34e9af2825363f59326f21693812ff2`；`npm install` 为 `found 0 vulnerabilities`；`npm run mysql:test` 返回 `ok=true`、表 `app_store`、`total=5`；PM2 `jiuzhuopanguan-backend` online；PM2 `pomer` 官网服务仍 online、uptime 10D、restarts 0。
- 线上公共健康：`GET https://api.pomer.cn/api/v1/config/home`、`GET https://api.pomer.cn/api/v1/config/templates`、`GET https://api.pomer.cn/admin/login` 均返回 200。
- 线上 `/sessions/live` 只读矩阵：无参 400；匿名 `sessionId=session-1782137141037-b4e84c` 401；匿名 `inviteCode=XBAABB` 200 且仅 13 个公开白名单字段；匿名 `sessionId+inviteCode` 401。
- 线上成员/非成员矩阵：成员 tokenTail `1dace82d` 下 `sessionId` 与 `inviteCode` 均 200，keyCount 29，`hasHostProfileId=true`、`hasJoinedPlayers=true`；非成员 tokenTail `b81a4c83` 下 `sessionId` 与 mixed 均 403，不含私有字段。
- 接口联调证据：`docs/runtime/pr-int-fix-014-01-20260623.md` 已补线上回归，结论为“本地合同通过，线上只读回归通过，QA 预览框/邀请加入交互待验收”。
- 服务器残留：部署前已存在的 `backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**` 为运行期数据，本次未清理；`npm install` 一度改动 `backend/package-lock.json`，PM 已恢复该锁文件到 Git 状态。
- 当前状态：`FIX-014-01` 后端/API 与接口联调线上只读门禁通过；仍需 QA 使用微信开发者工具预览框验收邀请加入交互、成员私有视图、匿名公开预览与异常态。未做真机，不得写正式准出。

### FIX-014-01 QA 验收派发

- 2026-06-24：PM 已向测试验收线程 `019eebc6-d497-7d80-9c6b-53254355fa69` 派发 `FIX-014-01-QA`。
- QA 目标文档：`docs/runtime/pr-qa-fix-014-01-20260624.md`。
- 验收要求：优先使用微信开发者工具预览框或 `npm.cmd run wechat:auto` 覆盖匿名无参不泄露、邀请码匿名公开预览不泄露私有字段、非成员/被踢/无权限不能读私有视图、合法成员读取与邀请加入不回归。
- 结论边界：只能写“预览框阶段通过 / blocked / 退回”；不得写正式真机准出。若 9420 或工具不可用，必须记录失败命令与错误原文。

### FIX-014-01 QA 退回

- 2026-06-24：测试验收负责人已完成 `FIX-014-01-QA`，证据 `docs/runtime/pr-qa-fix-014-01-20260624.md`，结论：退回。
- 自动化连接已恢复：旧版 automator 需使用 `cli.bat auto --project F:\codexlist\jiuzhuopanguan --auto-port 9420 --trust-project --debug`；`Test-NetConnection 127.0.0.1:9420` 返回 True，预览框自动化可用。连接方式已写入团队公告。
- QA 通过项：匿名首页不展示任意进行中聚会挂起条；匿名 `sessionId+inviteCode` 未绕过私有读取且不展示成员、照片、时间线、账本；合法成员邀请页和记录页读取正常，成员视图未回归。
- QA 退回项 `QA-FIX-014-01-R01`：匿名 `inviteCode` 进入邀请页或首页时先被登录态拦截，未展示后端 public live session 已允许的公开预览字段，如 `sessionName/playerCount/joinedCount`。
- QA 未覆盖项 `QA-FIX-014-01-R02`：测试未取得非成员或被踢用户的当前预览框 storage，不能把非成员/被踢预览框路径写通过。
- 当前状态：`FIX-014-01` 不能写“预览框阶段通过”或正式准出。下一步如要修补 `QA-FIX-014-01-R01`，需用户明确同意后再派前端负责人；PM 不直接改业务源码。

### FIX-014-01 QA 测试账号补充

- 2026-06-24：用户提醒使用旧测试账号，避免预览框被匿名登录态阻挡。
- PM 已复核当前线上 store：样本 `session-1782137141037-b4e84c / XBAABB` 下，成员有效 tokenTail 为 `1dace82d`；非成员有效 tokenTail 为 `b81a4c83`；备用非成员 tokenTail 为 `824395c3`、`923d75e4`、`0863fc6d`、`6d7d2743`。
- PM 已向测试验收线程补发指令：使用非成员但已登录的测试账号 tokenTail `b81a4c83` 注入微信开发者工具 storage，复测邀请码公开预览与私有视图阻断；如失效则改用备用非成员 tokenTail。
- 安全边界：完整 token 只能在临时脚本内从服务器当前 store 读取并写入 DevTools storage；文档、截图名、最终回复只允许写 token 后 8 位。

### FIX-014-01 QA 测试账号复测通过

- 2026-06-24：测试验收负责人已使用线上有效非成员测试账号 tokenTail `b81a4c83` 完成微信开发者工具预览框复测，证据仍为 `docs/runtime/pr-qa-fix-014-01-20260624.md`。
- 复测覆盖：`/pages/invite-group/index?inviteCode=XBAABB` 展示加入所需公开信息；`sessionId+inviteCode` mixed 入口和 `/pages/live-record/index?sessionId=session-1782137141037-b4e84c` 未展示目标成员、照片、时间线或账本。
- 新增截图：`docs/runtime/pr-qa-fix-014-01-nonmember-invite-public-clean-b81a4c83-20260624.png`、`docs/runtime/pr-qa-fix-014-01-nonmember-mixed-private-clean-b81a4c83-20260624.png`、`docs/runtime/pr-qa-fix-014-01-nonmember-live-record-direct-clean-b81a4c83-20260624.png`。
- 当前状态：`FIX-014-01` 预览框阶段通过，关闭首轮 `QA-FIX-014-01-R01/R02`；未做正式真机测试，不写正式发布准出。

## 2026-06-24 FIX-014-02 用户审批与派发

- 用户已明确同意 `FIX-014-02`；本轮只处理“分享页无权限仍展示聚会内容”的 P0 安全问题，其余 `FIX-014-*` 仍待逐项审批。
- 来源：`docs/runtime/uiux-full-interaction-audit-20260623-2102.md` 的 `UIUX-013-P0-01`，并复用 `docs/runtime/pr-uiux-share-auth-011.md` 的文案与按钮状态建议。
- 目标页面：`miniprogram/pages/share-poster/index`，重点阻断 `posterTimelineNodes`、照片、时间线、总结、二维码或分享图内容在 `notEnded/noPermission/removed/notMember/unavailable` 等状态下继续渲染。
- 工作树边界：PM 已从远端 `main` 新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-02`，分支 `codex/fix-014-02-share-permission`，避免主工作区历史脏改动和未确认提交混入。
- 已派发前端负责人 `019eebc6-99f6-71e2-83c4-e0c9d2afd6e2`：仅在该干净工作树内实施前端最小修复，证据文档为 `docs/runtime/pr-fe-fix-014-02-20260624.md`；完成后回报改动范围、验证命令、预览框证据或阻塞原文。
- 已派发后端/API 负责人 `019eebc6-f69d-73b2-a5d3-7f8e581283ac`：只读确认分享页相关接口错误类型与状态码合同，或指出缺口；不改业务源码、不提交、不部署，证据文档为 `docs/runtime/pr-backend-fix-014-02-contract-20260624.md`。
- 已预告测试验收负责人 `019eebc6-d497-7d80-9c6b-53254355fa69`：等待前端证据和后端/API 合同后再做预览框验收；未收到前端证据前保持 waiting，不得写通过。
- 当前状态：已审批、已派发，实施中。未取得前端证据、后端/API 合同确认和 QA 预览框证据前，`FIX-014-02` 不得写通过、部署完成或正式准出。

## 2026-06-24 FIX-014-03 用户审批与后端/API 本地合同

- 用户已用编号授权启动 `FIX-014-03`。本轮只处理“首拍进行中状态沉到后端”的后端/API 合同，不启动 `FIX-014-04` 前端统一消费或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-03`，分支 `codex/fix-014-03-first-photo-state`，基线为 `origin/main` 的 `bbca1be8`。
- 后端证据：`docs/runtime/pr-backend-fix-014-03-20260624.md`。
- 已实现合同：创建 session 默认 `hasFirstPhoto=false/isActiveForResume=false`；`createMoment()` 保存首张带 `imageUrl` 的完整照片后写回 `firstPhotoUploadedAt/hasFirstPhoto/isActiveForResume`；`/sessions/live`、`/user/session-moment-summaries`、后台 `sessions` 页面和 normalized read 路径输出同一字段。
- normalized 边界：本轮不做 `wine_sessions` DDL；normalized read 通过 `moment_records` 第一张有效图片推导首拍字段。若后续要求表级字段，需 DBA/后端补 DDL、迁移、回滚和线上验证。
- 本地验证通过：`node --check` 覆盖 `backend/server.js`、`backend/data/admin.js`、`backend/data/front.js`、`backend/data/moments.js`、`backend/data/normalized-read.js`、两个 smoke 脚本；`node backend/scripts/smoke-first-photo-active-state.js` 通过；`node backend/scripts/smoke-live-session-privacy.js` 通过；`npm.cmd run check:encoding` 通过；`npm.cmd run typecheck` 通过；`git diff --check` 通过。
- 当前状态：后端/API 本地合同通过；未提交、未推送、未部署、未上传小程序包。前端统一消费、后台页面人工复核、接口联调线上矩阵和 QA 预览框仍待补证，不得写线上通过或正式准出。

### FIX-014-02 前端与后端/API 证据复核

- 前端负责人已交付 `docs/runtime/pr-fe-fix-014-02-20260624.md`，改动集中在 `miniprogram/pages/share-poster/index.ts`、`index.wxml`、`index.less`。
- 前端实现：新增 `shareViewState/canViewPosterContent` 安全状态；`notEnded/noPermission/removed/notMember/unavailable` 阻断态下清空 `posterTimelineNodes`、照片、事件、账本摘要、二维码、ready 分享图 URL 和本地 poster 图路径；WXML 仅在 `canViewPosterContent=true` 时渲染时间线和正常保存/刷新/重试操作。
- 前端证据：`docs/runtime/pr-fe-fix-014-02-blocked-preview.png`、`docs/runtime/pr-fe-fix-014-02-cleared-preview.png`、`docs/runtime/pr-fe-fix-014-02-ready-preview.png`。其中阻断态截图覆盖“即使数据残留也不渲染内容”和“调用不可用状态后内容字段清空”；ready 截图仅覆盖非阻断状态，未覆盖真实 ready 分享图 URL 保存。
- 工作树纠偏：前端首次误写主工作区 `F:\codexlist\jiuzhuopanguan`，已停止使用主工作区作为本项来源；最终有效改动落在干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-02`。主工作区残留未纳入提交，后续需单独清理确认。
- 后端/API 负责人已交付 `docs/runtime/pr-backend-fix-014-02-contract-20260624.md`，结论为：`409 session not ended`、`403 not session member`、`403 forbidden`、`401 unauthorized`、`404 brief/share task not found`、无可展示节点 failed reason 均有可消费合同，可支撑前端阻断内容泄露。
- 后端/API 缺口：当前后端不能精确区分“普通非成员”和“被移出/被踢成员”，统一返回 `403 not session member`；因此本项只能验收“安全阻断内容”，不得写成后端已支持精准 `removed_from_session` 合同。
- PM 独立验证：`pwsh` 版本 `7.6.3`；`npm.cmd run typecheck` 通过；`npm.cmd run check:encoding` 通过；`git diff --check` 通过；WXML 三元字符串拼接在项目内已有同类用法，不作为退回项。
- 当前状态：前端本地实现与后端/API 合同复核通过，已具备 QA 预览框复测条件；`FIX-014-02` 仍未 QA 通过、未提交业务修复、未推送业务修复、未上传小程序包，不能写正式准出。

### FIX-014-02 QA 复测派发

- 2026-06-24：PM 已向测试验收负责人 `019eebc6-d497-7d80-9c6b-53254355fa69` 正式派发 `FIX-014-02-QA`。
- QA 目标文档：`docs/runtime/pr-qa-fix-014-02-20260624.md`。
- 复测工作树：`F:\codexlist\jiuzhuopanguan-fix-014-02`，分支 `codex/fix-014-02-share-permission`。
- 复测范围：未结束、无权限、非成员/被移出泛化态、任务不可用状态均不得展示照片、时间线、总结、二维码或分享图内容；按钮不得调用创建、刷新、重试或保存；合法 ready 非阻断路径不得回归。
- 结论边界：可写“预览框阶段通过 / blocked / 退回”；不得写正式真机发布准出。若真实 ready 分享图 URL 样本缺失，必须作为未覆盖项记录。

### FIX-014-02 QA 退回

- 2026-06-24：测试验收负责人已完成预览框复测，结论：退回。
- QA 证据：`docs/runtime/pr-qa-fix-014-02-20260624.md`。QA 首次写入主工作区 `F:\codexlist\jiuzhuopanguan`，后由 QA 移动到本项干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-02` 并清理主工作区误写；PM 已回读确认当前证据在正确 worktree。
- 通过项：注入式 `notEnded/noPermission/notMember/removed/unavailable` 阻断均能清空照片、时间线、总结、二维码、ready 图 URL 和本地 poster 图路径；保存/刷新/预览方法调用后不会恢复内容；ready 成员样本非阻断页面未崩。
- 退回项 `QA-FIX-014-02-R01`：真实非成员 tokenTail `b81a4c83` 直接打开 `/pages/share-poster/index?sessionId=session-1782137141037-b4e84c` 时，页面仍为 `shareViewState=ready`、`canViewPosterContent=true`、`shareActionBlocked=false`，并显示生成/刷新入口。即使没有泄露真实照片/时间线，也未进入安全阻断态，不符合验收口径。
- 未覆盖项 `QA-FIX-014-02-R02`：缺少真实 `readyShareImageUrl` 样本，未覆盖“合法成员、已结束且已有 ready 分享图 URL 的完整保存路径”。
- PM 初步原因判断：真实接口错误在 `loadBriefByQuery()` 等 catch 分支中先被 `toSafeShareErrorText()` 转成通用安全文案，再继续 `loadLiveSessionSummary()`，导致原始 `403 not session member` / `forbidden` 合同信息未进入 `shareViewState` 阻断映射。
- 当前状态：已派前端负责人返工 `FIX-014-02-FE-R01`；未通过 QA，不能提交业务修复、不能推送、不能上传小程序包、不能写正式准出。

### FIX-014-02 FE-R01 返工与当前阻塞

- 2026-06-24：前端负责人已交付 `FE-R01` 返工，证据追加在 `docs/runtime/pr-fe-fix-014-02-20260624.md`。
- 代码改动仍限 `miniprogram/pages/share-poster/index.ts`：新增 `getShareErrorText()`、`shouldBlockShareViewForError()`、`applyBlockedErrorTextState()`；补充 `not party member/not session player` 和中文安全文案“当前账号暂不能查看”“暂不能查看这张分享页”等映射；在 `loadBriefByQuery()`、`loadLiveSessionSummary()`、分享任务读取、刷新、保存等 catch 分支中先判阻断并 return，避免 fallback 恢复 ready。
- PM 独立命令验证：`npm.cmd run typecheck` 通过；`npm.cmd run check:encoding` 通过；`git diff --check` 通过。
- PM 截图复核：`docs/runtime/pr-fe-fix-014-02-r01-nonmember-b81a4c83.png`、`pr-fe-fix-014-02-r01-nonmember-b81a4c83-fixed.png`、`pr-fe-fix-014-02-r01-nonmember-b81a4c83-after.png` 均仍显示失败态或修补未完全生效阶段的正常分享页结构，不能作为通过证据。
- 预览框阻塞：PM 按公告固定流程执行 `pwsh -NoLogo -NoProfile -File scripts/start-wechat-devtools-automation.ps1 -Port 9420 -ProjectPath F:\codexlist\jiuzhuopanguan-fix-014-02 -QuitExisting`，端口监听恢复；但 `npm.cmd run wechat:auto -- status --port 9420` 仍失败，原文为 `/v2/auto returned a ticket, but no candidate WebSocket exposed currentPage/storage/screenshot/tap automation`。
- 当前状态：`FIX-014-02` 仍为 blocked / 待 QA 复测。前端代码静态门禁通过，但没有真实非成员 tokenTail `b81a4c83` 修补后预览框通过证据；不得提交、推送、部署、上传小程序包或写预览框阶段通过。
- 恢复条件：微信开发者工具自动化恢复到可执行 `currentPage/storage/screenshot/tap` 后，由 QA 使用 `docs/runtime/pr-fe-fix-014-02-20260624.md` 的 R01 证据重新复测 `QA-FIX-014-02-R01`；若仍失败，退回前端继续修。

### FIX-014-02 自动化修复与 R01 复测通过

- 2026-06-24：PM 复核发现自动化阻塞根因为脚本把启动参数误写为 `cli.bat auto --port 9420`，实际启动的是 IDE HTTP 服务，不是旧版 WebSocket 自动化端口；`wechat:auto status` 默认还会继续探测 `/v2/auto` ticket，造成“自动启动/自动激活”误判。
- 已修复自动化脚本：
  - `scripts/start-wechat-devtools-automation.ps1` 默认使用 `--auto-port 9420`，只有显式 `-AllowPortFallback` 才尝试旧 `--port` fallback。
  - `scripts/wechat-devtools-automator.js` 默认 `status` 改为 connect-only，不再自动 `/v2/auto` 探测或 `--launch` 拉起工具；`--probeV2` 与 `--launch` 均需显式传入。
  - storage 输出已对 `jzp-user-token` 与 `social-user-session-token` 脱敏，仅记录 `tokenTail`。
- 自动化验证：
  - `npm.cmd run wechat:auto -- status --port 9420`：默认 connect-only，可连接旧版 WebSocket 并读取 page/data/storage/screenshot；无可用 WS 时只报连接失败，不再触发 `/v2/auto`。
  - `npm.cmd run wechat:auto -- status --port 9420 --probeV2`：仅显式诊断时返回 `/v2/auto` ticket 信息。
  - `pwsh -NoLogo -NoProfile -File scripts/start-wechat-devtools-automation.ps1 -Port 9420 -ProjectPath F:\codexlist\jiuzhuopanguan-fix-014-02 -QuitExisting`：返回 `ok=true`、`mode=auto-port`。
- R01 真实非成员复测：使用非成员 tokenTail `b81a4c83`、profileId `user-1782177960380-f19492` 打开 `/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`，页面进入 `shareViewState=notMember`、`canViewPosterContent=false`、`shareActionBlocked=true`、`savePosterLabel=暂不可用`，照片/时间线/总结/二维码/ready 图 URL/本地 poster 图均为空；正常保存、分享保存、重试入口均不存在。截图：`docs/runtime/pr-qa-fix-014-02-r01-nonmember-retest-b81a4c83-20260624.png`。
- R01 合法成员回归：使用成员 tokenTail `1dace82d`、profileId `user-1782114038371-8b9a52` 打开同一路径，页面保持 `shareViewState=ready`、`canViewPosterContent=true`、`shareActionBlocked=false`，可见 2 条时间线照片、2 个照片高光、4 个账本高光和“生成聚会图”入口。截图：`docs/runtime/pr-qa-fix-014-02-r01-member-ready-retest-1dace82d-20260624.png`。
- 当前状态：`FIX-014-02` 预览框阶段 R01 已通过并关闭；`QA-FIX-014-02-R02` 仍未覆盖，因为当前线上样本没有真实 `readyShareImageUrl`，不能证明完整 ready 图 URL 保存链路。未做正式真机测试，不写正式发布准出。
- 交付边界：当前工作树仍未提交、未推送、未部署、未上传小程序包；提交前需保留本项变更范围与未跟踪证据清单，避免混入主工作区历史残留。

## 2026-06-24 FIX-014-04 用户审批与前端本地实现

- 用户已用编号授权启动 `FIX-014-04`。本轮只处理“首拍状态前端统一消费”，不启动 `FIX-014-05` 或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-04`，分支 `codex/fix-014-04-frontend-first-photo-state`，基线为 `origin/main` 的 `7111f058`。
- 前端证据：`docs/runtime/pr-fe-fix-014-04-20260624.md`。
- 已实现：`operations.ts` 暴露 `firstPhotoUploadedAt/hasFirstPhoto/isActiveForResume`；新增统一首拍状态工具；相册 ongoing 计数与列表共用分类；账本区分无聚会、待首拍、进行中、已结束；等待房间成员首拍前不自动进入记录页；首页和我的统计优先消费后端首拍合同。
- 本地验证通过：`npm.cmd run check:encoding`、`npm.cmd run typecheck`、`git diff --check`。`npm.cmd install` 为新工作树补齐依赖时报告既有 `10 vulnerabilities`，未做无关升级。
- 当前状态：前端实现已提交、推送并同步到服务器仓库，线上 HEAD `e6946388`；未上传小程序包，未做微信开发者工具预览框三路径复测；不得写正式准出。

## 2026-06-24 FIX-014-05 用户审批与推举资格文案

- 用户已用编号授权启动 `FIX-014-05`。本轮只处理“推举资格原因与前台文案”，不启动 `FIX-014-06` 或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-05`，分支 `codex/fix-014-05-nomination-reason-copy`，基线为 `origin/main` 的 `e6946388`。
- 证据：`docs/runtime/pr-fix-014-05-nomination-reason-20260624.md`。
- 已实现：后端推举资格接口输出稳定 `reasonCode/reasonText`；前端相册、聚会简报、今日回忆榜统一原因码文案；移除“照片审核通过后可推举”“待审核”等含混旧提示；保留内容安全确认、公开授权、私密可见性和榜单资格边界。
- 本地验证通过：`node --check backend/data/moments.js`、`node --check backend/server.js`、`node --check backend/scripts/smoke-nomination-eligibility-reasons.js`、`node backend/scripts/smoke-nomination-eligibility-reasons.js`、`npm.cmd run check:encoding`、`npm.cmd run typecheck`、`git diff --check`。
- 当前状态：已提交、推送并部署到 `api.pomer.cn` 对应 `jiuzhuopanguan-backend`，线上 HEAD `3d525db8`；部署证据见 `docs/runtime/deploy-fix-014-05-06-20260624.md`。未上传小程序包，未做线上真实推举样本和预览框复测；不得写正式准出。

## 2026-06-24 FIX-014-06 用户审批与前端本地实现

- 用户已用编号授权启动 `FIX-014-06`。本轮只处理“分享页来源感知返回”，不混入 `FIX-014-05` 或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-06`，分支 `codex/fix-014-06-share-poster-return`，基线为 `origin/main` 的 `e6946388`。
- 前端证据：`docs/runtime/pr-fe-fix-014-06-20260624.md`。
- 已实现：分享页顶部返回先按页面栈 `navigateBack`；无栈或失败时按 `from/returnMode/returnFilter` 回到分享图相册、历史相册筛选、我的、结果报告、简报或首页；底部独立按钮明确为“返回首页”；结束聚会、历史相册和结果报告入口补充来源参数；外部分享路径统一 `from=external`。
- 本地验证通过：`npm.cmd run check:encoding`、`npm.cmd run typecheck`、`git diff --check`。`npm.cmd install` 为新工作树补齐依赖时报告既有 `10 vulnerabilities`，未做无关升级。
- 当前状态：已提交、推送并同步部署到 `api.pomer.cn` 对应服务器仓库，线上 HEAD `3d525db8`；部署证据见 `docs/runtime/deploy-fix-014-05-06-20260624.md`。未做微信开发者工具预览框四路径点击验收，未上传小程序包，不得写正式发布准出。

## 2026-06-24 FIX-014-05 / FIX-014-06 提交部署收口

- Git：`FIX-014-05` 提交 `478b0e45 fix: clarify nomination eligibility reasons`；`FIX-014-06` 提交 `3d525db8 fix: preserve share poster return context`；均已推送到 `origin/main`。
- 部署目标：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`；未触碰 `pomer.cn` 公司官网目录、Nginx 或 PM2 `pomer` 服务。
- 服务器备份：`/www/backup/jiuzhuopanguan/fix-014-05-06-20260624053744`，已保存部署前 git 状态、HEAD、runtime diff、PM2 jlist、`server.js`、`moments.js` 和 `social-store.json`。
- 部署命令摘要：`git fetch origin main`、`git pull --ff-only origin main`、`node --check backend/server.js`、`node --check backend/data/moments.js`、`node --check backend/scripts/smoke-nomination-eligibility-reasons.js`、`cd backend && npm install`、`. ./.env && npm run mysql:test`、`pm2 restart jiuzhuopanguan-backend --update-env`。
- 部署验证：服务器 HEAD 从 `e6946388` fast-forward 到 `3d525db8`；`npm install` 返回 `found 0 vulnerabilities`；MySQL test 返回 `ok=true`、表 `app_store`、`total=5`；PM2 `jiuzhuopanguan-backend` online，PM2 `pomer` 官网服务 online 且 restart 仍为 `0`。
- 公网健康：`config/home` 200、`config/templates` 200、`admin/login` 200、`/sessions/live` 无参 400。
- 服务器脏项：`backend/package-lock.json` 曾被服务器 `npm install` 改动，已恢复到 Git 状态；部署后仅保留运行期 `backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**`。
- 当前边界：未上传微信小程序包；`FIX-014-05` 未做真实线上推举全链路验收；`FIX-014-06` 未做微信开发者工具预览框四路径点击验收；不得写正式发布准出。

## 2026-06-24 FIX-014-07 用户审批与本地实现

- 用户已用编号授权启动 `FIX-014-07`。本轮只处理“满员加入错误映射”，不启动 `FIX-014-08` 或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-07`，分支 `codex/fix-014-07-join-full-error`，基线为 `origin/main` 的 `782fe5f`。
- 证据：`docs/runtime/pr-fix-014-07-session-full-join-20260624.md`。
- 已实现：`/api/v1/sessions/join` 满员返回 HTTP 409 `session full`；`/api/v1/parties/join` 满员返回 HTTP 409 `party full`；首页口令加入对 `409/session full/party full/SESSION_FULL` 展示“聚会已满”，保留非名单成员和普通口令错误提示。
- 本地验证通过：`node --check backend/server.js`、`node --check backend/scripts/smoke-session-full-join-error.js`、`node backend/scripts/smoke-session-full-join-error.js`、`npm.cmd run check:encoding`、`npm.cmd run typecheck`、`git diff --check`。`npm.cmd install` 为新工作树补齐依赖时根目录报告既有 `10 vulnerabilities`，backend 为 `0 vulnerabilities`，未做无关升级。
- 当前状态：已提交、推送并部署到 `api.pomer.cn` 对应 `jiuzhuopanguan-backend`，线上代码 HEAD `a5c38bf9`；部署证据见 `docs/runtime/deploy-fix-014-07-20260624.md`。未上传小程序包；未做被踢重加和微信开发者工具预览框 QA；不得写正式发布准出。

## 2026-06-24 FIX-014-07 提交部署收口

- Git：提交 `a5c38bf9 fix: map full session joins to conflict` 已推送到 `origin/main`。
- 部署目标：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`；未触碰 `pomer.cn` 公司官网目录、Nginx 或 PM2 `pomer` 服务。
- 服务器备份：`/www/backup/jiuzhuopanguan/fix-014-07-20260624113938`，已保存部署前 git 状态、HEAD、runtime diff、PM2 jlist、`server.js` 和 `social-store.json`。
- 部署命令摘要：`git fetch origin main`、`git pull --ff-only origin main`、`node --check backend/server.js`、`node --check backend/scripts/smoke-session-full-join-error.js`、`cd backend && npm install`、`. ./.env && npm run mysql:test`、`pm2 restart jiuzhuopanguan-backend --update-env`。
- 部署验证：服务器 HEAD 从 `782fe5f` fast-forward 到 `a5c38bf9`；`npm install` 返回 `found 0 vulnerabilities`；MySQL test 返回 `ok=true`、表 `app_store`、`total=5`；服务器端 `node backend/scripts/smoke-session-full-join-error.js` 通过，`/sessions/join` 和 `/parties/join` 满员均为 HTTP 409；PM2 `jiuzhuopanguan-backend` online，PM2 `pomer` 官网服务 online 且 restart 仍为 `0`。
- 公网健康：`config/home` 200、`config/templates` 200、`admin/login` 200、`/sessions/live` 无参 400。
- 服务器脏项：`backend/package-lock.json` 曾被服务器 `npm install` 改动，已恢复到 Git 状态；部署后仅保留运行期 `backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**`。
- 当前边界：未上传微信小程序包；未做被踢重加和微信开发者工具预览框 QA；不得写正式发布准出。

## 2026-06-24 FIX-014-08 用户审批与本地实现

- 用户已用编号授权启动 `FIX-014-08`。本轮只处理“后台分享图重试权限合同”，不启动 `FIX-014-09` 或其他编号。
- 工作树边界：新建干净工作树 `F:\codexlist\jiuzhuopanguan-fix-014-08`，分支 `codex/fix-014-08-admin-share-retry-auth`，基线为 `origin/main` 的 `bc6d58ef`。
- 证据：`docs/runtime/pr-fix-014-08-admin-share-retry-auth-20260624.md`。
- 已实现：后台 `retryManagedShareImageTask()` 对齐用户端分享图合同，要求任务状态为 `failed/expired`、聚会已结束、brief/session 归属一致、可见节点非空；不保留超管绕过；拒绝和成功重试均写入 `operationLogs`。
- 本地验证通过：`node --check backend/data/admin.js`、`node --check backend/scripts/smoke-moments-flow.js`、`node --check backend/scripts/smoke-admin-moments-flow.js`、`node backend/scripts/smoke-moments-flow.js`、`node backend/scripts/smoke-admin-moments-flow.js`、`npm.cmd run check:encoding`、`npm.cmd run typecheck`、`git diff --check`。
- 依赖安装说明：新工作树补装根目录与 backend 依赖后完成验证；根目录 `npm.cmd install` 报告既有 `10 vulnerabilities`，backend `npm.cmd install` 为 `0 vulnerabilities`，本轮未做无关升级。
- 当前状态：已提交、推送并部署到 `api.pomer.cn` 对应 `jiuzhuopanguan-backend`，线上代码 HEAD `56e4b8f9`；部署证据见 `docs/runtime/deploy-fix-014-08-20260624.md`。未上传小程序包；未做后台页面人工点击复核和真实线上后台账号破坏性 retry；不得写正式发布准出。

## 2026-06-24 FIX-014-08 提交部署收口

- Git：业务提交 `3d7c76a3 fix: guard admin share task retries` 与测试稳定性提交 `56e4b8f9 test: isolate admin moments smoke store` 已推送到 `origin/main`。
- 部署目标：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`；未触碰 `pomer.cn` 公司官网目录、Nginx 或 PM2 `pomer` 服务。
- 服务器备份：`/www/backup/jiuzhuopanguan/fix-014-08-20260624120930`，已保存部署前 git 状态、HEAD、runtime diff、PM2 jlist、`admin.js`、`server.js`、`package-lock.json` 和 `social-store.json`。
- 部署命令摘要：`git fetch origin main`、`git pull --ff-only origin main`、`node --check backend/data/admin.js`、`node --check backend/scripts/smoke-moments-flow.js`、`node --check backend/scripts/smoke-admin-moments-flow.js`、`cd backend && npm install`、`. ./.env && npm run mysql:test`、两个服务器 smoke、`pm2 restart jiuzhuopanguan-backend --update-env`。
- 部署验证：服务器 HEAD 从 `bc6d58ef` fast-forward 到 `56e4b8f9`；`npm install` 返回 `found 0 vulnerabilities`；MySQL test 返回 `ok=true`、表 `app_store`、`total=5`；两个服务器 smoke 均通过；PM2 `jiuzhuopanguan-backend` online，PM2 `pomer` 官网服务 online 且 restart 仍为 `0`。
- 公网健康：`config/home` 200、`config/templates` 200、`admin/login` 200、`/sessions/live` 无参 400。
- 服务器脏项：`backend/package-lock.json` 曾被服务器 `npm install` 改动，已恢复到 Git 状态；部署后仅保留运行期 `backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**`。
- 当前边界：未上传微信小程序包；未做后台页面人工点击复核和真实线上后台账号破坏性 retry；不得写正式发布准出。
