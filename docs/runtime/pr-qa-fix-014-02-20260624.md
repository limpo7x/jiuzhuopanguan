# FIX-014-02-QA 分享页权限内容阻断验收

时间：2026-06-24（Asia/Shanghai）

角色：测试验收负责人

结论：退回

## 边界

- 仅在 `F:\codexlist\jiuzhuopanguan-fix-014-02` / `codex/fix-014-02-share-permission` 复测。
- 仅面向 `api.pomer.cn` / `jiuzhuopanguan`；未触碰 `pomer.cn` 官网。
- 未修改业务源码，未提交，未推送，未部署。
- 本轮只写 QA 证据，不写真机正式准出。

## 已读材料

- `docs/runtime/pr-fe-fix-014-02-20260624.md`
- `docs/runtime/pr-backend-fix-014-02-contract-20260624.md`
- `docs/runtime/pm-active-worklog.md`
- `docs/gameplay-moments-team-announcements.md`
- `miniprogram/pages/share-poster/index.ts`
- `miniprogram/pages/share-poster/index.wxml`
- `miniprogram/pages/share-poster/index.less`

## 自动化环境

启动 9420 自动化：

```text
D:\wechatkaifa\微信web开发者工具\cli.bat auto --project F:\codexlist\jiuzhuopanguan-fix-014-02 --auto-port 9420 --trust-project --debug
√ auto
```

确认连接：

```text
Test-NetConnection 127.0.0.1:9420 -> TcpTestSucceeded=True
npm.cmd run wechat:auto -- --storage ... -> ok=true
projectPath=F:\codexlist\jiuzhuopanguan-fix-014-02
```

使用账号只记录 token 后 8 位：

- 成员：`1dace82d`
- 非成员：`b81a4c83`

## 前端证据截图复核

| 截图 | QA 复核 |
| --- | --- |
| `docs/runtime/pr-fe-fix-014-02-blocked-preview.png` | 画面为未结束阻断态，未看到照片、时间线、二维码或分享图内容；与前端说明一致。 |
| `docs/runtime/pr-fe-fix-014-02-cleared-preview.png` | 画面为未结束阻断态，文案更完整；与数据清空说明方向一致。 |
| `docs/runtime/pr-fe-fix-014-02-ready-preview.png` | 画面为 ready/预览态，仍显示正常生成/刷新入口；该样本没有真实 ready 分享图 URL，只能证明非阻断页面未崩，不能证明“合法成员已结束 ready 分享图保存路径”完整通过。 |

## 注入阻断态复测

方法：进入 `/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`，先注入敏感占位数据，再调用页面方法 `applyPosterUnavailableState(<message>)`，随后调用 `handleSaveTap()`、`handleTaskPrimaryTap()`、`handlePreviewTaskTap()`，确认不会恢复可见内容。

注入前敏感占位包括：

- `posterTimelineNodes`
- `photoHighlights`
- `keyEvents`
- `accountingHighlights`
- `shareSummary`
- `qrCodeImageUrl`
- `readyShareImageUrl`
- `posterImagePath`
- `posterImageUrl`
- `ledgerSummary`
- `ledgerRankings`

### notEnded

触发：`session not ended`

截图：`docs/runtime/pr-qa-fix-014-02-notEnded-blocked-20260624.png`

结果：

- `shareViewState=notEnded`
- `canViewPosterContent=false`
- `shareActionBlocked=true`
- `posterTimelineNodes/photoHighlights/keyEvents/accountingHighlights` 均为 `0`
- `shareSummary/qrCodeImageUrl/readyShareImageUrl/posterImagePath/posterImageUrl` 均为空
- 调用保存/刷新/预览方法后仍保持阻断态和空内容

判定：通过。

### noPermission

触发：`forbidden`

截图：`docs/runtime/pr-qa-fix-014-02-noPermission-blocked-20260624.png`

结果：内容字段全部清空，`shareViewState=noPermission`，保存/刷新/预览方法调用后仍不恢复照片、时间线、总结、二维码或分享图内容。

判定：通过。

### notMember

触发：`not session member`

截图：`docs/runtime/pr-qa-fix-014-02-notMember-blocked-20260624.png`

结果：内容字段全部清空，`shareViewState=notMember`，保存/刷新/预览方法调用后仍不恢复照片、时间线、总结、二维码或分享图内容。

判定：通过。

### removed

触发：`removed member`

截图：`docs/runtime/pr-qa-fix-014-02-removed-blocked-20260624.png`

结果：前端可在收到 `removed/kicked` 文案时进入 `removed` 阻断态并清空内容。

边界：后端/API 合同明确当前不能精确区分普通非成员和被移出成员，线上不能证明后端已支持精准 `removed_from_session`。

判定：前端映射通过；后端精准 removed 合同未覆盖，不作为本项通过条件。

### unavailable

触发：`share task not found`

截图：`docs/runtime/pr-qa-fix-014-02-unavailable-blocked-20260624.png`

结果：内容字段全部清空，`shareViewState=unavailable`，保存/刷新/预览方法调用后仍不恢复照片、时间线、总结、二维码或分享图内容。

判定：通过。

## ready 非阻断回归

路径：`/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`

账号：成员 tokenTail `1dace82d`

截图：`docs/runtime/pr-qa-fix-014-02-ready-member-1dace82d-20260624.png`

页面 data 摘要：

```json
{
  "shareViewState": "ready",
  "canViewPosterContent": true,
  "shareActionBlocked": false,
  "posterTimelineNodes": 2,
  "photoHighlights": 2,
  "accountingHighlights": 4,
  "readyShareImageUrl": "",
  "savePosterLabel": "生成聚会图",
  "displayTaskStatus": "预览中"
}
```

判定：ready 非阻断页面未回归，照片/时间线数据可展示。

未覆盖：该样本没有真实 `readyShareImageUrl`，未覆盖“合法成员、已结束且已有 ready 分享图 URL 的完整保存路径”。

## 真实非成员入口复测

路径：`/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`

账号：非成员 tokenTail `b81a4c83`

截图：`docs/runtime/pr-qa-fix-014-02-nonmember-direct-b81a4c83-20260624.png`

页面 data 摘要：

```json
{
  "shareViewState": "ready",
  "canViewPosterContent": true,
  "shareActionBlocked": false,
  "posterTimelineNodes": 0,
  "photoHighlights": 0,
  "accountingHighlights": 0,
  "shareSummary": "这场聚会的照片、账本和朋友回忆都会在这里汇总。",
  "savePosterLabel": "生成聚会图",
  "displayTaskStatus": "预览中"
}
```

结果：

- 未展示真实照片、时间线、账本或 ready 分享图 URL。
- 但没有进入 `noPermission/notMember` 安全状态。
- 页面仍渲染正常分享页结构和生成/刷新入口：`canViewPosterContent=true`、`shareActionBlocked=false`、`savePosterLabel=生成聚会图`。

判定：退回。真实非成员直达分享页应进入安全阻断态，不能保持 ready 非阻断态。

## Console / Network

- 自动化捕获 Console 数量：`0`。
- 本轮未捕获新增阻塞型 Console 错误。
- 9420 和 DevTools 可用，无自动化阻塞。

## 退回项

| 编号 | 责任 | 问题 | 缺少的证据 / 修复要求 |
| --- | --- | --- | --- |
| `QA-FIX-014-02-R01` | 前端 | 真实非成员 tokenTail `b81a4c83` 直接打开 `/pages/share-poster/index?sessionId=session-1782137141037-b4e84c` 时，页面仍为 `shareViewState=ready`、`canViewPosterContent=true`、`shareActionBlocked=false`，并显示生成/刷新入口。 | 非成员/无权限真实接口路径必须在页面加载阶段进入 `noPermission/notMember` 或等价安全状态，隐藏正常生成/刷新/保存入口，只保留安全解释和返回动作；补预览框截图和 data 证据。 |
| `QA-FIX-014-02-R02` | 前端 / PM | 当前缺真实 ready 分享图 URL 样本。 | 若要验收“合法成员已结束 ready 分享图不能回归”的完整保存链路，需要提供真实 ready 分享图 URL 样本；本轮只覆盖 ready 非阻断页面未崩和内容可展示。 |

## 最终结论

退回。

注入式 `notEnded/noPermission/notMember/removed/unavailable` 阻断逻辑通过，ready 非阻断页面未崩；但真实非成员直达分享页没有进入安全阻断态，仍展示正常生成/刷新入口，因此不能写“预览框阶段通过”。

## R01 修复后预览框复测补充

时间：2026-06-24

### 自动化恢复说明

本轮先修复了微信开发者工具自动化脚本误用启动参数的问题：

- `scripts/start-wechat-devtools-automation.ps1` 默认改为 `cli.bat auto --auto-port 9420`，不再用 `--port 9420` 启动 IDE HTTP 服务。
- `scripts/wechat-devtools-automator.js` 默认 `status` 只连接既有旧版 WebSocket 自动化端口；不再自动探测 `/v2/auto` ticket，也不自动拉起开发者工具。
- 只有显式传入 `--probeV2` 才做 `/v2/auto` 诊断；只有显式传入 `--launch` 才脚本拉起工具。
- storage 输出已对 `jzp-user-token` 和 `social-user-session-token` 做脱敏，只记录 `tokenTail`。

恢复后 `npm.cmd run wechat:auto -- status --port 9420` 可读取当前页面、data、storage 与截图；默认模式不再出现 ticket-only 自动启动误判。

### R01 真实非成员复测

路径：`/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`

账号：非成员 tokenTail `b81a4c83`，profileId `user-1782177960380-f19492`

截图：`docs/runtime/pr-qa-fix-014-02-r01-nonmember-retest-b81a4c83-20260624.png`

页面 data 摘要：

```json
{
  "shareViewState": "notMember",
  "canViewPosterContent": false,
  "shareActionBlocked": true,
  "errorText": "当前账号已不在这场聚会中，暂不能查看分享图",
  "savePosterLabel": "暂不可用",
  "permissionTitle": "当前账号不能查看这张分享图",
  "posterTimelineNodes": 0,
  "photoHighlights": 0,
  "accountingHighlights": 0,
  "shareSummary": "",
  "qrCodeImageUrl": "",
  "readyShareImageUrl": "",
  "posterImagePath": "",
  "posterImageUrl": ""
}
```

WXML 选择器复核：

| 选择器 | 结果 |
| --- | --- |
| `[data-save-selector="share-flow-015-save-poster"]` | 不存在 |
| `[data-share-save-selector="share-flow-015-save"]` | 不存在 |
| `.poster-action-retry` | 不存在 |
| `.poster-permission-actions` | 存在 |
| `.poster-permission-card` | 存在 |

判定：`QA-FIX-014-02-R01` 关闭。真实非成员直达分享页已进入安全阻断态，不展示照片、时间线、总结、二维码、ready 分享图或正常生成/刷新/保存/重试入口。

### R01 合法成员回归复测

路径：`/pages/share-poster/index?sessionId=session-1782137141037-b4e84c`

账号：成员 tokenTail `1dace82d`，profileId `user-1782114038371-8b9a52`

截图：`docs/runtime/pr-qa-fix-014-02-r01-member-ready-retest-1dace82d-20260624.png`

页面 data 摘要：

```json
{
  "shareViewState": "ready",
  "canViewPosterContent": true,
  "shareActionBlocked": false,
  "errorText": "",
  "savePosterLabel": "生成聚会图",
  "posterTimelineNodes": 2,
  "photoHighlights": 2,
  "accountingHighlights": 4,
  "shareSummary": "这场聚会留下 2 张公开照片、0 条账本高光和 0 个关键时刻。",
  "readyShareImageUrl": ""
}
```

判定：合法成员 ready 非阻断页面未回归，照片和时间线可展示，正常生成入口可见。

### R01 后结论

预览框阶段通过。

- `QA-FIX-014-02-R01`：已通过并关闭。
- `QA-FIX-014-02-R02`：仍未覆盖。当前线上样本没有真实 `readyShareImageUrl`，不能证明“合法成员、已结束且已有 ready 分享图 URL 的完整保存路径”。
- 本结论只代表微信开发者工具预览框阶段通过；未做正式真机发布准出。
