# FIX-014-08 提交推送与部署记录

日期：2026-06-24

目标边界：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`。未改动、未重启、未代理 `pomer.cn` 公司官网服务。

## Git

- 业务提交：`3d7c76a3 fix: guard admin share task retries`
- 测试稳定性提交：`56e4b8f9 test: isolate admin moments smoke store`
- 均已推送到 `origin/main`。
- 部署时代码 HEAD：`56e4b8f9cd314f32ff862f7d894ba09ffb55ab95`

## 部署

- 服务器目录：`/www/wwwroot/jiuzhuopanguan-git`
- 部署前 HEAD：`bc6d58ef483260803f8f9fcfb477b26eec7f0af8`
- 部署后 HEAD：`56e4b8f9cd314f32ff862f7d894ba09ffb55ab95`
- 服务器备份目录：`/www/backup/jiuzhuopanguan/fix-014-08-20260624120930`
- 部署方式：`git fetch origin main` + `git pull --ff-only origin main`

## 服务器验证

- `node --check backend/data/admin.js`：通过。
- `node --check backend/scripts/smoke-moments-flow.js`：通过。
- `node --check backend/scripts/smoke-admin-moments-flow.js`：通过。
- `cd backend && npm install`：通过，`found 0 vulnerabilities`。
- `. ./.env && npm run mysql:test`：通过，`ok=true`、表 `app_store`、`total=5`。
- `node backend/scripts/smoke-moments-flow.js`：通过，覆盖后台 retry 未结束拒绝、无可见节点拒绝、合格任务恢复和 operationLogs。
- `node backend/scripts/smoke-admin-moments-flow.js`：通过，覆盖后台 HTTP 409、合格任务恢复和操作日志页可见。
- `pm2 restart jiuzhuopanguan-backend --update-env`：通过。
- PM2 `jiuzhuopanguan-backend`：online，pid `829346`，restart `96`。
- PM2 `pomer` 官网服务：online，pid `2699`，restart `0`，未重启。

## 公网健康检查

- `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- `https://api.pomer.cn/api/v1/config/templates`：HTTP 200。
- `https://api.pomer.cn/admin/login`：HTTP 200。
- `https://api.pomer.cn/api/v1/sessions/live` 无参：HTTP 400，符合当前隐私合同。

## 服务器工作树

- `backend/package-lock.json` 曾被服务器 `npm install` 改动，已恢复到 Git 状态。
- 部署后仅保留运行期数据和上传物脏项：`backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**`。

## 未覆盖

- 未上传微信小程序包。
- 未做后台页面人工点击复核。
- 未用真实线上后台账号执行破坏性 retry；服务器 smoke 已用隔离 fixture 覆盖成功与拒绝合同。
