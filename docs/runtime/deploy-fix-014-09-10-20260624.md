# FIX-014-09 / FIX-014-10 提交推送与部署记录

日期：2026-06-24

目标边界：仅 `api.pomer.cn` / `/www/wwwroot/jiuzhuopanguan-git` / PM2 `jiuzhuopanguan-backend`。未改动、未重启、未代理 `pomer.cn` 公司官网服务。

## Git

- `FIX-014-09` 提交：`ccf7b8b1 fix: require auth for user commerce`
- `FIX-014-10` 提交：`6dc147c1 fix: route admin session state repairs`
- 均已推送到 `origin/main`。
- 部署时代码 HEAD：`6dc147c1edd98a33097e67987901cc9c95c9fbc9`

## 部署

- 服务器目录：`/www/wwwroot/jiuzhuopanguan-git`
- 部署前 HEAD：`8924ef4cb7f76fd2043d469990bef679a40a7c90`
- 部署后 HEAD：`6dc147c1edd98a33097e67987901cc9c95c9fbc9`
- 服务器备份目录：`/www/backup/jiuzhuopanguan/fix-014-09-10-20260624124734`
- 部署方式：`git fetch origin main` + `git pull --ff-only origin main`

## 服务器验证

- `node --check backend/server.js`：通过。
- `node --check backend/data/admin.js`：通过。
- `node --check backend/scripts/smoke-user-commerce-auth.js`：通过。
- `node --check backend/scripts/smoke-admin-session-state-repair.js`：通过。
- `cd backend && npm install`：通过，`found 0 vulnerabilities`。
- `. ./.env && npm run mysql:test`：通过，`ok=true`、表 `app_store`、`total=5`。
- `node backend/scripts/smoke-user-commerce-auth.js`：通过，未登录 `/user/commerce` 为 401，登录 token 为 200。
- `node backend/scripts/smoke-admin-session-state-repair.js`：通过，覆盖 sessions 只读、直接保存 409、repair-state end/resume、report/brief/share task 同步。
- `pm2 restart jiuzhuopanguan-backend --update-env`：通过。
- PM2 `jiuzhuopanguan-backend`：online，pid `831816`，restart `97`。
- PM2 `pomer` 官网服务：online，pid `2699`，restart `0`，未重启。

## 公网健康检查

- `https://api.pomer.cn/api/v1/config/home`：HTTP 200。
- `https://api.pomer.cn/api/v1/config/templates`：HTTP 200。
- `https://api.pomer.cn/admin/login`：HTTP 200。
- `https://api.pomer.cn/api/v1/sessions/live` 无参：HTTP 400，符合当前隐私合同。
- `https://api.pomer.cn/api/v1/user/commerce` 未登录：HTTP 401。

## 服务器工作树

- `backend/package-lock.json` 曾被服务器 `npm install` 改动，已恢复到 Git 状态。
- 部署后仅保留运行期数据和上传物脏项：`backend/data/social-store.json`、`backend/backups/`、`backend/public/uploads/**`。

## 未覆盖

- 未上传微信小程序包。
- 未做微信开发者工具预览框 QA。
- 未用真实后台账号执行破坏性 session 状态修复；服务器 smoke 已用隔离 fixture 覆盖合同和同步行为。
