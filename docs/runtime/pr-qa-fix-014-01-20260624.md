# FIX-014-01-QA `/api/v1/sessions/live` 隐私修复验收

时间：2026-06-24（Asia/Shanghai）

角色：测试验收负责人

结论：预览框阶段通过

## 边界

- 只验收已部署到 `api.pomer.cn` 的 `/api/v1/sessions/live` 隐私修复。
- 未修改业务源码，未提交，未推送，未部署。
- 未触碰 `pomer.cn` 公司官网。
- 本轮不写真机准出。

## 已读材料

- `docs/runtime/pr-int-fix-014-01-20260623.md`
- `docs/runtime/pm-audit-fix-approval-plan-20260623-213518.md` 中 `FIX-014-01`
- `docs/runtime/pm-active-worklog.md` 中 `FIX-014-01` 最新条目
- `miniprogram/services/operations.ts`
- `miniprogram/pages/index/index.ts`
- `miniprogram/pages/invite-group/index.ts`
- `miniprogram/pages/waiting-room/index.ts`
- `miniprogram/pages/live-record/index.ts`

## 自动化环境

首次连接 9420 失败：

```text
npm.cmd run wechat:auto
connect ECONNREFUSED 127.0.0.1:9420
```

开发者工具实际 HTTP server 在 `127.0.0.1:20394`：

```text
IDE server has started on http://127.0.0.1:20394 and must be restarted on port 9420 first
```

改用旧版 automator 需要的 `--auto-port` 后恢复 9420：

```text
D:\wechatkaifa\微信web开发者工具\cli.bat auto --project F:\codexlist\jiuzhuopanguan --auto-port 9420 --trust-project --debug
√ auto
```

随后：

```text
Test-NetConnection 127.0.0.1:9420 -> TcpTestSucceeded=True
npm.cmd run wechat:auto -- --storage ... -> ok=true
```

当前预览框登录态 token 只记录后 8 位：`1dace82d`。

## 只读 HTTP 辅助抽查

这些结果只能作为辅助证据，不替代预览框结论。

| 场景 | 请求 | 结果 |
| --- | --- | --- |
| 匿名无参 live | `GET https://api.pomer.cn/api/v1/sessions/live` | `400 sessionId or inviteCode required` |
| 匿名直接 `sessionId` | `GET /api/v1/sessions/live?sessionId=session-1782137141037-b4e84c` | `401 unauthorized` |
| 匿名仅 `inviteCode` | `GET /api/v1/sessions/live?inviteCode=XBAABB` | `200`，仅返回公开字段：`hostName/id/inviteCode/joinedCount/partyId/playerCount/sessionName/stateText/status/subtitle/templateImageUrl/templateName/title` |
| 匿名 `sessionId + inviteCode` | `GET /api/v1/sessions/live?sessionId=session-1782137141037-b4e84c&inviteCode=XBAABB` | `401 unauthorized` |

辅助判断：后端已部署的接口只读合同符合接口联调文档，不再匿名返回任意进行中聚会。

## 预览框复测证据

### 1. 匿名首页

命令：自定义 automator 脚本清空 storage 后 `reLaunch('/pages/index/index')`。

截图：`docs/runtime/pr-qa-fix-014-01-anon-home-20260624.png`

结果：

- `loggedIn=false`
- `activeSessionCount=0`
- `sessionReturn.visible=false`
- 首页不展示任意进行中聚会挂起条。

判定：通过。

### 2. 匿名邀请码邀请页公开预览

命令：清空 storage 后 `reLaunch('/pages/invite-group/index?inviteCode=XBAABB')`。

截图：`docs/runtime/pr-qa-fix-014-01-anon-invite-public-20260624.png`

页面 data 摘要：

```json
{
  "sessionId": "",
  "inviteCode": "XBAABB",
  "joinedCount": 0,
  "playerCount": 0,
  "avatarSlots": [],
  "joinStatusText": "邀请已准备好"
}
```

观察：

- 页面没有泄露成员列表、照片、时间线、账本。
- 但页面也没有展示后端匿名公开接口已允许的 `sessionName/playerCount/joinedCount` 等公开预览字段。
- 代码复核显示 `miniprogram/pages/invite-group/index.ts` 在调用 `hydrateLiveSession(sessionId, inviteCode)` 前先执行 `ensureUserAuthorized(...)`；匿名用户未授权时直接返回，因此预览框没有实际拉取匿名 public live session。

首轮判定：退回前端。该结论已被后续“使用非成员测试账号复测”覆盖，当前以第 6 节 `b81a4c83` 复测结果为准。

### 3. 匿名首页带 inviteCode

命令：清空 storage 后 `reLaunch('/pages/index/index?inviteCode=XBAABB')`。

截图：`docs/runtime/pr-qa-fix-014-01-anon-index-invite-20260624.png`

页面 data 摘要：

```json
{
  "loggedIn": false,
  "authPanelVisible": true,
  "userName": "未登录",
  "activeSessionCount": 0,
  "sessionReturn.visible": false
}
```

观察：

- 首页 inviteCode 路径直接弹登录面板。
- 未泄露私有视图。
- 但同样没有匿名公开预览信息。

首轮判定：退回前端公开预览链路。该结论已被后续“使用非成员测试账号复测”覆盖，当前以第 6 节 `b81a4c83` 复测结果为准。

### 4. 匿名 `sessionId + inviteCode` 不绕过私有读取

命令：清空 storage 后 `reLaunch('/pages/invite-group/index?sessionId=session-1782137141037-b4e84c&inviteCode=XBAABB')`。

截图：`docs/runtime/pr-qa-fix-014-01-anon-direct-private-blocked-20260624.png`

页面 data 摘要：

```json
{
  "sessionId": "session-1782137141037-b4e84c",
  "inviteCode": "XBAABB",
  "joinedCount": 0,
  "playerCount": 0,
  "avatarSlots": []
}
```

结果：

- 未展示成员列表、照片、时间线、账本。
- 与 HTTP `sessionId + inviteCode -> 401` 方向一致，没有通过 inviteCode 绕过私有读取。

判定：通过。

### 5. 合法成员邀请页 / 记录页回归

命令：恢复 storage 后，使用 tokenTail `1dace82d` 进入：

- `/pages/invite-group/index?sessionId=session-1782137141037-b4e84c&inviteCode=XBAABB`
- `/pages/live-record/index?sessionId=session-1782137141037-b4e84c`

截图：

- `docs/runtime/pr-qa-fix-014-01-member-invite-private-20260624.png`
- `docs/runtime/pr-qa-fix-014-01-member-live-record-20260624.png`

结果：

- 邀请页：`joinedCount=2`、`playerCount=2`、`avatarSlots` 有 2 位成员。
- 记录页：`memberCountText=2/2 人`，`players` 有 2 位成员，时间线照片 `imageBroken=false`。

判定：通过。合法成员私有视图与记录页读取未回归。

## Console / Network

- Automator console 捕获到 6 条 `info` 级日志，主要为 `session-exit enableAlertBeforeUnload enabled` 与记录页照片诊断。
- 未捕获新增阻塞型 Console 错误。
- HTTP 辅助抽查中未出现新增 `404 / 5xx`。
- 匿名私有读取的 `401`、匿名无参的 `400` 属于本次预期合同。

## 首轮未覆盖项

- 未覆盖真实非成员 token 的预览框私有视图读取。
- 未覆盖真实被踢用户 token 的预览框私有视图读取。
- 接口联调文档已给出非成员 tokenTail `b81a4c83` 的线上只读结果，但 QA 本轮未取得该账号 storage，因此不能把该项写成预览框通过。
- 未做真机测试，不作为正式真机发布准出。

上述非成员预览框缺口已在后续“使用非成员测试账号复测”中用 tokenTail `b81a4c83` 覆盖。独立“被踢后重新进入”账号样本本轮未单独覆盖，但非成员 / 无权限私有视图阻断已通过预览框验证；接口联调线上矩阵已覆盖非成员 `403`。

## 使用非成员测试账号复测

PM 补充后，按线上 store 当前有效测试账号注入微信开发者工具 storage。

使用账号：

- tokenTail：`b81a4c83`
- profileId：`user-1782177960380-f19492`
- 完整 token 仅在 PowerShell / Node 进程内用于写入 DevTools storage，未写入文档、最终回复或截图文件名以外位置。

远端 tokenTail 查询命令采用 PM 指定 here-string 管道方式：

```powershell
$script | ssh pomer.cn 'set -e; cd /www/wwwroot/jiuzhuopanguan-git/backend; set -a; . ./.env; set +a; node -'
```

查询摘要：

```json
{
  "ok": true,
  "tokenTail": "b81a4c83",
  "profileId": "user-1782177960380-f19492"
}
```

### 6. 非成员登录态：邀请码公开预览

路径：`/pages/invite-group/index?inviteCode=XBAABB`

截图：`docs/runtime/pr-qa-fix-014-01-nonmember-invite-public-clean-b81a4c83-20260624.png`

页面 data 摘要：

```json
{
  "sessionId": "session-1782137141037-b4e84c",
  "inviteCode": "XBAABB",
  "joinedCount": 2,
  "playerCount": 2,
  "sessionName": "卐卍",
  "avatarSlots": [
    { "filled": false, "name": "待加入", "profileId": "" },
    { "filled": false, "name": "待加入", "profileId": "" }
  ],
  "joinStatusText": "2/2 位好友已加入",
  "currentProfileId": "user-1782177960380-f19492",
  "isJudge": false
}
```

结果：

- 展示加入所需公开信息：`sessionName / playerCount / joinedCount / inviteCode`。
- 不展示成员真实列表、成员头像、照片、时间线、账本。
- 成员槽位为 `待加入` 空占位，`profileId` 为空。

判定：通过。

### 7. 非成员登录态：`sessionId + inviteCode` mixed 入口

路径：`/pages/invite-group/index?sessionId=session-1782137141037-b4e84c&inviteCode=XBAABB`

截图：`docs/runtime/pr-qa-fix-014-01-nonmember-mixed-private-clean-b81a4c83-20260624.png`

页面 data 摘要：

```json
{
  "sessionId": "session-1782137141037-b4e84c",
  "inviteCode": "XBAABB",
  "joinedCount": 0,
  "playerCount": 2,
  "sessionName": "卐卍",
  "avatarSlots": [
    { "filled": false, "name": "待加入", "profileId": "" },
    { "filled": false, "name": "待加入", "profileId": "" }
  ],
  "joinStatusText": "等待好友加入",
  "currentProfileId": "user-1782177960380-f19492",
  "isJudge": false
}
```

结果：

- 未展示目标 session 成员真实列表、成员头像、照片、时间线、账本。
- 与接口联调 `sessionId + inviteCode` 非成员 `403` 方向一致，预览框未泄露私有视图。

判定：通过。

### 8. 非成员登录态：记录页直接入口

路径：`/pages/live-record/index?sessionId=session-1782137141037-b4e84c`

截图：`docs/runtime/pr-qa-fix-014-01-nonmember-live-record-direct-clean-b81a4c83-20260624.png`

页面 data 摘要：

```json
{
  "sessionId": "session-1782137141037-b4e84c",
  "playerCount": 2,
  "sessionName": "卐卍",
  "players": [
    {
      "name": "非成员测试账号",
      "profileId": "user-1782177960380-f19492"
    }
  ],
  "memberCountText": "1/2 人",
  "records": [
    {
      "name": "非成员测试账号",
      "profileId": "user-1782177960380-f19492"
    }
  ],
  "recordTimelineDisplayItems": [],
  "activeSegment": "record"
}
```

结果：

- 未展示目标 session 的真实成员列表。
- 未展示目标 session 的照片、时间线、账本。
- 页面以当前非成员测试账号的本地空记录态展示，未泄露被保护 session 的私有视图。

判定：通过。可后续由前端优化无权限 UX，但不作为本次隐私修复阻塞。

### 非成员复测 Console

- Console 捕获 18 条 `info` 级日志，均为 `session-exit enableAlertBeforeUnload enabled`。
- 未捕获新增阻塞型 Console 错误。
- 未观察到新增阻塞型 `401 / 404 / 5xx` 页面异常；非成员私有读取阻断符合预期。

## 历史退回项处理

| 编号 | 责任 | 问题 | 需要补齐 |
| --- | --- | --- | --- |
| `QA-FIX-014-01-R01` | 前端 | 首轮匿名 `inviteCode` 被登录态拦截，未展示公开字段。 | PM 补充要求改用已登录非成员测试账号；`b81a4c83` 复测已展示公开字段且无私有泄露，当前关闭。 |
| `QA-FIX-014-01-R02` | PM / 接口联调 / 测试 | 首轮未取得非成员当前预览框 storage。 | 已使用线上 store 有效非成员 tokenTail `b81a4c83` 注入复测，当前关闭。 |

最终结论：预览框阶段通过。后端 HTTP 隐私合同、合法成员读取回归、非成员登录态公开预览和私有视图阻断均通过本轮抽查；未做正式真机发布准出。
